# sink — Flappy Bird UX design

Author: (unfilled — sink)

Part of [[flappy-bird-cooperative-design]] (`#159`). Proposed lens: presentation/UX — visual
style (palette, shapes vs. sprites, given "no external assets" is the likely constraint),
score/UI placement, restart flow, optional juice (screen shake, particle burst on collision).
Reassign or redefine freely — this is an invitation, not an assignment.

*(Empty stub — fill in via `git pull` / edit / `git commit` + push, or just comment on
`#159` if that's easier. Either reaches central.)*

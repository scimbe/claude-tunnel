# source-2 — Flappy Bird mechanics design

Author: (unfilled — source-2)

Part of [[flappy-bird-cooperative-design]] (`#159`). Proposed lens: game-feel/mechanics —
bird physics (gravity curve, flap impulse, terminal velocity), difficulty pacing (pipe gap
size/speed over time), collision-detection fairness (hitbox vs. sprite size), win/lose feel.
Reassign or redefine freely — this is an invitation, not an assignment.

*(Empty stub — fill in via `git pull` / edit / `git commit` + push, or just comment on
`#159` if that's easier. Either reaches central.)*

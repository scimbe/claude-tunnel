# Build Progress Log

Driven by the `/loop` process (`DEVELOPMENT-PROCESS.md` D1–D8): one Task Packet per cycle, each green increment committed. The loop reads this file to know what's next.

## Packet status

| Packet | Status | Notes |
|--------|--------|-------|
| P0.1 workspace + CI + docker | ✅ done | 4 crates, `cargo build/test --workspace` green in `rust:1-slim` |
| P0.2 `common` wire-types | ✅ done | TenantId, AgentId, RoutingToken, OriginIdentity, Capability, ControlFrame + serde round-trip tests |
| P1.1 Edge QUIC listener | ✅ done | P1.1a endpoint/cert · P1.1b connect+echo · P1.1c reject untrusted — 4 ct-edge tests |
| P1.2 Agent dialer + TCP fallback | 🔨 in progress | P1.2a ✅ (selection + QUIC dialer + interop) · P1.2b (reconnect) · P1.2c (TCP fallback transport) |
| P1.3 join-token enrollment | ✅ done | P1.3a service + P1.3b agent ed25519 identity + enroll interop |
| P1.4 short-lived mTLS auth | ✅ done | full auth chain over QUIC: enroll → mint → present → verify (P1.4a–d) |

**🎯 Milestone 1 complete** (P0.1–P1.4 core): authenticated QUIC transport between Agent and Edge, backed by enrollment + short-lived credentials. Deferred enhancements: P1.2b (reconnect), P1.2c (HTTP/2-over-TCP fallback).

**🎯 Milestone 2 complete** (relay-first path): P2.1 ✅ (Tunnel Registry) · P2.2 ✅ (Capability register) · P2.3 ✅ (P2.3a token resolution) · P2.4 ✅ (P2.4a relay primitive + P2.4b QUIC relay). Deferred: NAT hole-punching (P2P direct path), full end-to-end wiring.

**🎯 Milestone 3 complete** (Noise Client↔Origin E2E, SPEC §10 item 3): P3.1 ✅ (keypair) · P3.2 ✅ (Noise_IK handshake) · P3.3 ✅ (framing + **E2E through relay: edge sees only ciphertext**) · P3.4 ✅ (Capability import + handshake pinning). Provider-blind mesh crypto demonstrated end to end.

**🎯 Milestone 4 complete** (PoW-gated rendezvous, SPEC §10 item 5, ADR-0018): P4.1 ✅ (PoW) · P4.2 ✅ (gate helpers + QUIC gate) · P4.3 ✅ (per-token rate limiting). Anti-abuse layer live on the wire.

**Milestone 5** (Docker testbed): M5.1 ✅ (Edge daemon) · M5.2 ✅ (Agent daemon: config · `register_tunnel` · `serve_stream_to_origin` · `run_agent` loop — dial+register+accept-serve, tested end to end; `main.rs` cert-load wiring folds into M5.4/5.5) · M5.3 ✅ (Client) · M5.4 ✅ (image `ct-testbed`) · M5.4b ✅ (unified serve+tunnel e2e) · M5.4c ✅ (all three mains wired) · **🎯 Milestone 5 essentially complete** — M5.5a ✅ (compose e2e smoke GREEN) · M5.5b ✅ (**`tc netem`**: `netem-entrypoint.sh` + `NET_ADMIN`; tunnel round-trips under `EDGE_DELAY=30ms EDGE_LOSS=1%`, `COMPOSE_EXIT=0`) · M5.5c (NAT): the relay path already traverses NAT via the Agent's *outbound* dial — P2P hole-punching (which a NAT would stress) isn't implemented in code (deferred in the design), so a NAT-behind-relay demo is optional. · M5.6 smoke ✅ (achieved by M5.5a). **Next: M6 performance evaluation** (netem sweep → latency/throughput → plots for the thesis).

**Milestone 6** (performance evaluation): M6.1 ✅ (summarize + run_bench + csv_row) · M6.1c ✅ (client bench mode, live @20ms → mean 92.9ms) · M6.2 ✅ (`scripts/sweep.sh` → `latency.csv`; 0ms→7.1ms, 20ms→95.6ms) · M6.3 ✅ (`plot.py`+`plot.sh` → `latency-vs-delay.png`, `latency-vs-loss.png`) · M6.4 ✅ (**`scripts/tabulate.py`** → `results-table.{md,tex}` + written analysis `results.md`; real 3×2 matrix: RT ≈ 6.8 + 4.2·delay, 2% loss grows the p95 tail at 50ms 260→307ms). **Milestone 6 (Performance-Evaluation) complete** — sweep→CSV→plot→table→analysis, all docker-only.

**Milestone 7** (BA thesis, German, HAW-konform, texlive-in-docker): M7.1 ✅ (**LaTeX scaffold** — `docs/thesis/thesis.tex` `scrbook`, HAW title page, Eidesstattliche Erklärung, Kurzfassung+Abstract, 6 chapter stubs, BibLaTeX/biber; `docker/thesis.Dockerfile` minimal TeX Live + `scripts/thesis-build.sh`; compiles to `thesis.pdf` in-container, 0 undefined refs, M6 results table embedded → `Tabelle 5.1`) · M7.2a ✅ (**Einleitung** — Motivation/Problemstellung, Zielsetzung, 3 Forschungsfragen [Realisierbarkeit/Latenz-Overhead/Netzverhalten], Aufbau; sourced from SPEC §1–3/§9 + ADRs, honest about scope: relay path + PoW, Noise as building block) · M7.2b ✅ (**Grundlagen** — 2.1 providerblinde Relays/ZK-Prinzip, 2.2 QUIC [RFC 9000/9001], 2.3 Noise `Noise_IK`/X25519-Pinning, 2.4 Proof-of-Work [Hashcash]; cited, from ADR-0004/0013/0018 + CONTEXT.md). **M7.2 komplett** · M7.3 ✅ (**Architektur** — 3.1 Komponenten/Topologie mit TikZ-Diagramm [Abb. 3.1: Client→Edge→Agent→Origin, Control Plane, providerblinder Noise-E2E-Bogen], 3.2 Schlüsselflüsse, 3.3 Rollen-Dispatch `'A'`/`'C'`, 3.4 Entwurfsentscheidungen; from SPEC §4–5 + ADRs) · M7.4 ✅ (**Implementierung** — 4.1 Workspace [5-crate table], 4.2 ct-common Bausteine + Listing 4.1 PoW, 4.3 Rollen-Dispatch + Listing 4.2 `serve_connection`, 4.4 Agent/Client+Bench; `listings` pkg; from `crates/*`, "94 Tests" claim empirically re-verified TOTAL_PASSED=94) · M7.5 ✅ (**Evaluation** — 5.1 Testbett/Methodik, 5.2/5.3 Ergebnisse [Tabelle 5.1 + Abb. 5.1/5.2 eingebettet], 5.3 Diskussion beantwortet FF2 [RT≈6.8+4.2·d] & FF3 [2% Verlust → p95-Tail +18% bei 50ms], 5.4 Limitierungen; aus results.md) · M7.6 ✅ (**Fazit + Ausblick** — 6.1 Zusammenfassung [FF1 realisierbar für Architektur+Kernmechanismen, offen: Noise-E2E am Live-Pfad; FF2/FF3-Recap], 6.2 Ausblick [Prototyp-Lücken: P2P-Hole-Punching, Live-Noise-E2E, TCP-Fallback, belastbarere Eval; offene Risiken + zurückgestellte Features aus BACKLOG.md]). **Milestone 7 (BA-Thesis) komplett** — 21-seitiges PDF, kompiliert sauber im texlive-Container.

### 🏁 Geplanter DAG (M1–M7) vollständig
Rust-Tunnel (94 Tests), Docker+netem-Testbett, Mess-Pipeline mit echten Daten, komplette deutsche BA-Thesis (PDF). Verbleibende **SPEC §8 v1-Features noch nicht entwickelt** (Basis für DAG-Erweiterung): direkter P2P-Datenpfad (NAT-Hole-Punching, Relay nur als Fallback), Noise-E2E auf dem Live-Pfad verankert, HTTP/2-über-TCP-Fallback.

**Milestone 8** (DAG-Erweiterung — Noise-E2E am Live-Pfad, sodass der Edge nur Chiffretext relayt): M8.1 ✅ (**`ct-agent::origin::OriginKey`** — Agent hält das Origin-Noise-Static-Keypair; `main` mintet die Capability mit der echten OriginIdentity statt `[0u8;32]`; Test beweist, dass die geprägte Identity + der zurückbehaltene Private-Key einen vollständigen `Noise_IK`-Handshake abschließen) · M8.2 ✅ (**`ct-client::noise::client_noise_exchange`** — stream-generischer `Noise_IK`-Initiator: framed Handshake + verschlüsselte Nutzlast, pinnt `cap.origin`; Tests: verschlüsselter Roundtrip gegen einen Responder + Fehlschlag bei falscher Origin Identity) · M8.3 ✅ (**`ct-agent::serve::serve_noise_bridge`** — stream-generischer `Noise_IK`-Responder via `origin_handshake(private)`; entschlüsselt die Client-Frame, leitet Klartext an den lokalen Origin (TCP) weiter, verschlüsselt die Antwort zurück; Test: inline-Initiator ↔ Bridge ↔ echter TCP-Echo-Origin) · M8.4a ✅ (**`ct-client::transport::client_tunnel_noise`** — Rendezvous+PoW dann `client_noise_exchange` über denselben QUIC-Stream; **funktionaler E2E-Test**: Client → echter Edge-`serve_connection`-Relay → Agent-`serve_noise_bridge` → echter TCP-Echo-Origin → zurück, Noise-verschlüsselt, `secret-payload` kommt an) · M8.4b ✅ (**Provider-Blind-Assertion, Client-Sende-Seite** — `RecordingWriter`/`RecordingReader` fangen exakt die Tunnel-Wire-Bytes ab, die der Edge byteweise relayt [`relay_quic` = reiner Copy]; `client_noise_exchange` ↔ Responder ↔ echter Origin, dann Assertion: die relayten Bytes enthalten den Klartext-Marker nie; ergänzt den bereits vorhandenen Edge-Relay-Test `noise_e2e_through_relay_edge_sees_only_ciphertext`) · M8.4c-i ✅ (**Agent-live-Pfad auf Noise** — `run_agent(..., origin_private)` serviert relayte Streams via `serve_noise_bridge`; agent `main` reicht `origin_key.private_bytes()` durch; Integrationstest umgeschrieben auf Noise-Initiator) · M8.4c-ii ✅ (**Client-live-Pfad auf Noise** — `bench::run_once`/`run_bench` nehmen jetzt `cap`+`client_private` und rufen `client_tunnel_noise`; client `main` generiert ein Client-Keypair und nutzt in Single- und Bench-Modus den Noise-Tunnel; Bench-Test auf Noise-Responder-Agent + echten Multi-Accept-Origin umgeschrieben) · M8.4c-iii ✅ (**Compose-Smoke über Noise** — `docker compose up` mit den neuen Binaries: `ct-client: tunnel round-trip OK`, `sent/received "hello-tunnel"`, `COMPOSE_EXIT=0`; der gesamte Container-Stack fährt den verschlüsselten Live-Pfad E2E). **Milestone 8 (Noise-E2E am Live-Pfad) komplett** — Thesis-Fazit-Lücke geschlossen.

### 🎯 Neues Ziel (User-Direktive): VOLLES Produkt, alle SPEC §8 v1-Features + alle E2E-Tests
Gap-Analyse gegen Code → Roadmap in `v1-first-task-packets.md`: **M9** genereller Streaming-Datenpfad · **M10** UDP-Origins · **M11** direkter P2P-Pfad + Relay-Fallback · **M12** HTTP/2-über-TCP-Fallback · **M13** gehosteter Control-Plane-Service · **M14** Agent-seitige Observability · **M15** pseudonyme Accounts + Krypto-Payment. Jedes Milestone landet mit frozen E2E-Test durch echte Komponenten/Container. Loop treibt eins pro Zyklus.

### 📄 BA-Thesis-Neugestaltung (User-Direktive, Zyklus 75)
Die M7-Thesis (21 S., `docs/thesis/thesis.tex`) wird **ersetzt**: zu flach, falsche Vorlage. Neu: **offizielle HAW-Vorlage** (heruntergeladen+extrahiert nach `docs/thesis/haw-template/`), **~64 Seiten**, tiefere/besser formulierte Analysen, und die **Experimente/Parameterstudie ans Ende** (erst Produkt M11–M15 fertig, dann Studie am fertigen Produkt). Neue Milestones **M16** (umfassende Parameterstudie) + **M17** (tiefe Thesis) — Details in `v1-first-task-packets.md`.

**M15** (pseudonyme Accounts + Krypto-Payment, ADR-0012, SPEC §9): M15.1 ✅ (**Prepaid-Credit-Ledger** — `ct-control-plane::accounts::Ledger` [In-Memory, kein Trust-Material]: opaker zufälliger `AccountId(32 B)` [Pseudonymität, keine PII], `open_account` [Start-Balance 0], `balance`, `credit` [saturierend → kein Wrap durch feindliche Top-ups], `debit` [schlägt bei zu wenig Guthaben mit `InsufficientCredit{balance,requested}` fehl, **ohne** die Balance zu verändern]. `LedgerError::{UnknownAccount,InsufficientCredit}` mit Display/Error. 5 Tests: distinkte Ids+Null-Start, credit→debit-Verlauf, verweigerter Debit unverändert, Null-Balance kann nicht ausgeben, Unknown-Account-Fehler. **Anmerkung**: pseudonym+prepaid stoppt keinen *finanzierten* Sybil-Angreifer — bleibt offenes Risiko [BACKLOG.md]) · M15.2 ⏳ Token-/Capability-Ausgabe an Guthaben gekoppelt · M15.3 ⏳ Krypto-Payment-Intake-Stub (Top-up).

**M14** (Agent-seitige Observability, ADR-0016): M14.1a ✅ (**`ct-common::metrics`** — dependency-freies Metrik-Set: `Counter` [`AtomicU64`, `Relaxed`] + `TunnelMetrics` {tunnels_opened/failed, bytes_to_origin/to_client, handshakes, handshake_millis_total}; `observe_handshake(Duration)` inkrementiert Count + Latenz-Summe [Scraper bildet Mittel = millis_total/handshakes]; `render_prometheus()` gibt Prometheus-Text-Exposition [`# HELP`/`# TYPE counter`/value] aus. 4 Unit-Tests: Counter inc/add/get, Handshake-Akkumulation, Render-Format, thread-shared Summierung [8×1000]) · M14.1b-i ✅ (**`Metered<S>`-Byte-Zähler-Adapter** — `AsyncRead`+`AsyncWrite`-Wrapper, der bei jedem erfolgreichen `poll_read`/`poll_write` den jeweiligen Counter hochzählt und sonst transparent an den inneren Stream durchreicht; wird um den Origin-Socket gelegt → keine Änderung an `noise_pump`. `TunnelMetrics`-Counter sind jetzt `Arc<Counter>` [Struct `Clone`], sodass eine einzelne Serie unabhängig an den Wrapper übergeben werden kann. Test: `duplex`-Paar, 5 B geschrieben → `write`-Counter=5, 3 B empfangen → `read`-Counter=3) · M14.1b-ii ✅ (**Datenpfad instrumentiert** — `serve_noise_stream(…, metrics: Arc<TunnelMetrics>)`: Handshake mit `Instant` gemessen [in einen `async`-Block gekapselt, damit Erfolg/Fehler gezählt werden kann]; bei Erfolg `observe_handshake(elapsed)` + `tunnels_opened.inc()`, bei Fehler `tunnels_failed.inc()`. Origin-Socket in `Metered` gewickelt: Reads vom Origin → `bytes_to_client`, Writes zum Origin → `bytes_to_origin`. `serve_direct` reicht die Metrics an den Tcp-Arm durch; `run_agent` baut **eine** geteilte `Arc<TunnelMetrics>` und gibt Clones an alle Serve-Tasks [Signatur unverändert → kein Ripple auf `main.rs`]. Auch die ct-client-Test-Helfer angepasst. Test [100 KB E2E]: `tunnels_opened=1`, `handshakes=1`, `bytes_to_origin=bytes_to_client=100000`, `tunnels_failed=0`). **M14.1 komplett.** · M14.2a ✅ (**`/metrics`-Endpoint** — neues Modul `ct-agent::observe`: `metrics_router(Arc<TunnelMetrics>)` [axum] mit `GET /metrics` → `render_prometheus()` als `text/plain; version=0.0.4`; `serve_metrics(addr, metrics)` bindet+serviert. `AgentConfig.metrics_listen: Option<SocketAddr>` aus `CT_AGENT_METRICS_LISTEN`; `run_agent` spawnt den Endpoint neben der Serve-Loop, wenn gesetzt [teilt dieselbe `Arc<TunnelMetrics>` → kein run_agent-Signatur-Refactor nötig]. Tests: `tower::oneshot` [Render+Content-Type+Werte] + realer Socket-Scrape [ephemerer Port, roher HTTP/1.0-GET → `ct_tunnels_opened_total 3`]) · M14.2b ✅ (**Compose-Scrape-Smoke** — Overlay `docker/docker-compose.metrics.yml`: Agent-Env `CT_AGENT_METRICS_LISTEN=0.0.0.0:9100`, Client-Command auf `sh -c "ct-client && metrics_probe"` überschrieben [Tunnel dann Scrape im selben Container → sein Exit ist das Probe-Verdikt, kein Abort-Race durch den Client-Exit]. Neues Bin `crates/agent/src/bin/metrics_probe.rs` [roher HTTP/1.0-GET über tokio-TCP, keine neuen Deps]: pollt `CT_AGENT_METRICS_ADDR` bis `ct_tunnels_opened_total >= 1`. Dockerfile kopiert `metrics_probe` ins Image. Live: `ct-client: tunnel round-trip OK (via=quic)` → `metrics probe OK: ct_tunnels_opened_total=1 ct_bytes_to_origin_total=12 via 10.5.0.4:9100`, `client exited with code 0`, **COMPOSE_EXIT=0**, sauberer Teardown [Container+Volume+Netz entfernt]). **🎯 Milestone 14 (Observability) komplett** — der Agent exponiert live Prometheus-Metriken, die Zähler bewegen sich nachweislich bei Tunnel-Aktivität. **Next: M15** (pseudonyme Accounts + Krypto-Payment) — letztes v1-Feature-Milestone.

**M13** (gehosteter Control-Plane-Service): M13.1 ✅ (**Enrollment-HTTP-API** — `ct-control-plane::http::enrollment_router` [axum]: `POST /enroll/issue` {tenant}→{token hex}, `POST /enroll/redeem` {token,agent,pubkey hex}→{tenant} bzw. 409; über die bestehende In-Memory-`Enrollment`; Tests via `tower::oneshot`) · M13.2 ✅ (**Registry-/Rendezvous-Endpoints** — `registry_router`: `POST /registry/register` {token,tenant,agent}, `GET /registry/resolve/:token` → {tenant,agent} bzw. 404 [die Rendezvous-Auflösung]; über `TunnelRegistry`; Test register→resolve→unbekannt=404) · M13.3 ✅ (**Service-Binary** — `control_plane_router` merged Enrollment+Registry; `ct-control-plane`-Bin serviert via `axum::serve` auf `CT_CONTROL_PLANE_LISTEN` [Default `0.0.0.0:8090`]; Test: merged Router bedient beide Routen) · M13.4a ✅ (**HTTP-Client `ControlPlaneClient`** [reqwest, plaintext] — `issue_join_token`/`redeem`/`register`/`resolve` gegen den *laufenden* Service; `CpError::{Http,Status,Malformed}`. **Live-Service-E2E**: der echte `control_plane_router` wird via `axum::serve` auf einem ephemeren TCP-Port gebunden, dann fährt der Client die volle Kette issue→redeem→register→resolve über einen echten Socket [+ unbekannter Token = 404-Fehler, Join-Token-Reuse = 409-Fehler]) · M13.4b ✅ (**Compose-Smoke** — `docker/docker-compose.controlplane.yml` [standalone]: `ct-control-plane`-Container serviert auf `0.0.0.0:8090`, ein zweiter Container `cp_selftest` [neues Bin `crates/control-plane/src/bin/cp_selftest.rs`, Retry bis Service oben] fährt die volle Kette gegen den **laufenden Container**: enroll [issue→redeem]→register→resolve; Dockerfile kopiert jetzt `ct-control-plane` + `cp_selftest` ins Runtime-Image. Live: `control-plane selftest OK: enroll -> register -> resolve (tenant-smoke, agent-smoke)`, `selftest exited with code 0`, `COMPOSE_EXIT=0`, sauberer Teardown). **🎯 Milestone 13 (gehosteter Control-Plane-Service) komplett** — die In-Memory-Library läuft jetzt als containerisierter Dienst, E2E gegen den laufenden Service belegt. **Next: M14** (Agent-seitige Observability — Prometheus/OTel `/metrics`).

**M12** (HTTP/2-über-TCP-Fallback, wenn UDP blockiert): M12.1 ✅ (**`ct-agent::transport::probe_udp_reachable`** — QUIC-Handshake-Versuch mit Timeout → `bool`, Input für `select_transport` [Quic vs TcpFallback]; Tests: erreichbarer Edge→Quic, toter UDP-Port→TcpFallback) · M12.2a ✅ (**TLS-über-TCP-Transport** — `ct-edge::transport::build_tcp_tls_listener_at` [TcpListener + `TlsAcceptor` + self-signed Cert] + `tcp_tls_connect(addr, cert) -> TlsStream<TcpStream>`; `tokio-rustls` ergänzt; Echo-Test. **Design-Hinweis**: TLS-TCP trägt das transport-agnostische Tunnel-Protokoll [Noise-Frames via `noise_pump`], literales HTTP/2-Framing = spätere Härtung [wie ed25519-Credential≈mTLS]) · M12.2b ✅ (**`ct-edge::serve::serve_tcp_connection`** — servt `'C'`-Rendezvous [Challenge+PoW] über einen einzelnen TLS-TCP-Stream und relayt ihn zum QUIC-Agent; **Cross-Transport-Relay** [TLS-TCP-Client ↔ QUIC-Agent] via generisches `relay`; E2E-Test grün) · M12.2c ✅ (**`ct-client::transport::client_tunnel_noise_tcp`** — `'C'`-Rendezvous+PoW über den TLS-TCP-Stream, dann `split` + `client_noise_exchange`; voller Noise-Tunnel über den TCP-Fallback; E2E-Test: Client→TLS-TCP→Edge→QUIC-Agent→Noise→Origin→Echo) · M12.3a ✅ (**Dual-Transport-Edge** — `build_dual_edge(quic_addr, tcp_addr)` baut QUIC-Endpoint + TLS-TCP-Listener mit **einem geteilten** self-signed Cert; `run_edge` lauscht jetzt auf beiden [QUIC-Primary + TCP-Fallback-Accept-Loop mit `serve_tcp_connection`]; Test: QUIC+TLS-TCP verbinden beide mit demselben Cert) · M12.3b ✅ (**Client-`main`-Fallback** — `tcp_tls_connect` in ct-client; Single-Modus: QUIC-Probe [2s-Timeout um `dial_edge`] → bei Erfolg QUIC-Tunnel, sonst `tcp_tls_connect`+`client_tunnel_noise_tcp`; `CT_CLIENT_FORCE_TCP` erzwingt Fallback; loggt `via=quic|tcp`; Connector-Test grün) · M12.3c ✅ (**Compose-UDP-blockiert-Smoke** — `docker/docker-compose.tcp.yml`: Client-Entrypoint `iptables -A OUTPUT -p udp -d 10.5.0.2 -j DROP` blockt sein ausgehendes UDP zum Edge; Live: `tunnel round-trip OK (via=tcp)`, `COMPOSE_EXIT=0` — QUIC-Probe scheitert → TLS-TCP-Fallback → Edge relayt TCP-Client zu QUIC-Agent). **Milestone 12 (TCP-Fallback) komplett** — der Fallback greift live, wenn UDP blockiert ist.

**M11** (direkter P2P-Pfad + Relay-Fallback — schwierigstes Milestone, NAT-Traversal): M11.1 ✅ (**`EdgeState` Peer-Candidate-Registry** — speichert je Token die vom Edge beobachtete reflexive Adresse des Agents; `register_with_candidate`/`candidate`, `remove` räumt beides; 3 Unit-Tests) · M11.2 ✅ (**Candidate bei Registrierung aufgezeichnet** — `serve_connection` `'A'` + `register_agent` nutzen jetzt `register_with_candidate(token, conn, conn.remote_address())`; Interop-Test prüft `candidate(token).is_some()` nach Registrierung) · M11.3a ✅ (**`'P'`-Peer-Candidate-Query** — neuer Rollen-Byte-Zweig in `serve_connection`: Client sendet `'P'|token`, Edge antwortet mit dem längen-präfixierten Candidate-String (len 0 = keiner); Client-`query_peer_candidate` → `Option<SocketAddr>`; separat vom `'C'`-Relay-Pfad, bricht nichts; Interop-Test grün) · M11.3b ✅ (**Agent-Direct-Path-Listener** — `ct-agent::transport::build_direct_listener[_at]`: QUIC-Server-Endpoint mit self-signed Cert für direkte Client-Verbindungen [umgeht den Edge-Relay]; rcgen ergänzt, ct-agent bleibt unabhängig von ct-edge; Test: Client vertraut Cert, verbindet direkt, Echo) · M11.3c ✅ (**Client-Direct-Connect + Direct-Tunnel** — `client_direct_connect(candidate, agent_cert, timeout)` [Timeout→Fallback] + `client_tunnel_direct` [Noise ohne `'C'`/PoW, direkt zum Agent-Listener]; E2E-Test: Client→Agent-Listener→Noise→Origin, **ohne Edge**) · M11.4a ✅ (**`client_tunnel_p2p_or_relay`** — versucht Direct, fällt bei Timeout/Fehler auf den Edge-Relay zurück; gibt `(used_direct, response)`; Test: unerreichbarer Direct-Candidate → Relay liefert) · M11.4b-i ✅ (**EdgeState `advertise_direct`/`direct_endpoint`** — speichert je Token die vom Agent advertisierte Direct-Listener-`(SocketAddr, cert-DER)`; `remove` räumt auch das; 2 Unit-Tests) · M11.4b-ii ✅ (**`'D'`-Advertise + `'P'`-Discover** — Edge-`'D'`-Verb speichert `advertise_direct`, `'P'` gibt jetzt `[1]addr_len·addr·cert_len·cert` (oder `[0]`); `ct-agent::transport::advertise_direct_listener`, `ct-client::transport::query_direct_endpoint -> Option<(SocketAddr, CertificateDer)>`; Test: Agent advertised → Client fragt ab → exakt `(addr,cert)` zurück) · M11.4b-iii ✅ (**`ct-agent::serve::serve_direct`** — Accept-Loop auf dem Direct-Listener, serviert jede Direct-Verbindung via `serve_noise_stream`/`serve_noise_udp` (proto-gewählt); Test: inline-Client verbindet direkt, Noise-Roundtrip zum Origin) · M11.4b-iv ✅ (**`client_tunnel_auto` + voller P2P-E2E** — Client fragt Direct-Endpoint via `'P'` ab, dann `client_tunnel_p2p_or_relay`; **E2E-Test**: Agent advertised Listener + `serve_direct` → Client auto-discovered via Edge → verbindet **direkt** zum Agent, `used_direct=true`. **Ehrliche Lücke geschlossen** [Auto-Discovery+Cert-Verteilung+Direktpfad] auf Integrationsebene) · M11.4b-v ✅ (**`run_agent`-Daemon-Wiring** — `AgentConfig.direct_advertise_ip: Option<IpAddr>` aus `CT_AGENT_DIRECT_ADVERTISE`; wenn gesetzt: `build_direct_listener` → advertise `(ip:port, cert)` an den Edge (separate Conn) → `serve_direct` gespawnt neben der Relay-Schleife; `None` = reiner Relay) · M11.4b-vi ✅ (**Compose-P2P-Smoke** — `docker/docker-compose.p2p.yml`: Agent `CT_AGENT_DIRECT_ADVERTISE=10.5.0.4`, Client `CT_CLIENT_MODE=p2p`→`client_tunnel_auto`; Live-Ergebnis: `p2p received "hello-tunnel" (direct=true)`, `COMPOSE_EXIT=0` — der Client **auto-discovered** den Agent-Listener und tunnelt **direkt** am Edge vorbei). **Milestone 11 (direkter P2P-Pfad + Relay-Fallback) komplett** — Direktpfad live im Container-Stack; echtes Hole-Punching bleibt dokumentierte Testbett-Grenze (flaches Bridge-Netz ohne NAT).

**M10** (UDP-Origins): M10.1 ✅ (**`ct-agent::serve::serve_noise_udp`** — Responder-Handshake, dann bidirektionale Datagramm-Bridge: **eine Noise-Frame = ein UDP-Datagramm** [Framing erhält Datagramm-Grenzen], `select!`-Teardown bei Tunnel-Close; Test: 3 diskrete Datagramme durch echten UDP-Echo-Origin, Grenzen+Inhalt erhalten) · M10.2 ✅ (**`ct-client::transport::client_tunnel_udp`** — Rendezvous+Handshake + Datagramm-Bridge lokaler UDP-Socket ↔ Origin; **UDP-E2E durch den echten Edge**: lokale UDP-App → `client_tunnel_udp` → Edge-Relay → Agent `serve_noise_udp` → echter UDP-Echo-Origin, 3 diskrete Datagramme mit Grenzen erhalten [ct-client 16 Tests grün]) · M10.3 ✅ (**Agent-Protokollwahl** — `AgentConfig.origin_proto` (`OriginProto::{Tcp,Udp}` aus `CT_AGENT_ORIGIN_PROTO`, Default Tcp); `run_agent` verzweigt `serve_noise_stream` vs `serve_noise_udp`; Config-Parse-Tests) · M10.4 ✅ (**Client-`main`-UDP-Modus** — `CT_CLIENT_MODE=udp` → `udp_selftest` [bindet lokalen UDP-Socket, sendet 1 Datagramm über `client_tunnel_udp`, prüft Echo]; UDP-E2E-Test auf `udp_selftest` umgestellt → 108 Tests grün) · M10.5 ✅ (**Compose-UDP-Smoke** — `docker/docker-compose.udp.yml`-Overlay: `socat UDP4-LISTEN` Echo-Origin [antwortet vom Port 8080, damit der connected Agent-Socket es annimmt] + Agent `origin_proto=udp` + Client `MODE=udp`; `docker compose … up` → `ct-client: UDP tunnel round-trip OK`, `udp sent/received "hello-tunnel"`, exit 0). **Milestone 10 (UDP-Origins) komplett** — UDP tunnelt voll live im Container-Stack.

**M9** (Streaming-Datenpfad): M9.1 ✅ (**`ct-common::noise::noise_pump`** — bidirektionaler Streaming-Noise-Transport: Klartext↔verschlüsselte Frames in beide Richtungen konkurrent, `TransportState` unter kurz-gehaltenem Mutex; + `read_frame` in ct-common; tokio-io-util-Dep ergänzt; Test: 200 KB Multi-Frame-Roundtrip durch zwei gekoppelte Pumps) · M9.2 ✅ (**`ct-agent::serve::serve_noise_stream`** — Responder-Handshake + `noise_pump` zwischen Client-Stream und Origin-TCP-Socket, full-duplex streaming; Test: 100 KB durch Bridge → echter Streaming-Echo-Origin) · M9.3 ✅ (**`ct-client::transport::client_tunnel_stream`** — Rendezvous+PoW + Initiator-Handshake + `noise_pump` über den QUIC-Stream, bridged eine lokale App zum Origin; **E2E-Test: 150 KB bidirektional durch den echten Edge+Agent+Origin**) · M9.4 ✅ (**live-Pfad auf Streaming** — `run_agent` serviert relayte Streams jetzt via `serve_noise_stream`; der One-Shot-Client bleibt interoperabel [Streaming-Origins echoen ohne EOF-Wait]; alle 105 Tests grün + **Compose-Smoke** `round-trip OK`/`COMPOSE_EXIT=0`; `serve_noise_bridge` bleibt als getesteter One-Shot-Baustein). **Milestone 9 (Streaming-Datenpfad) komplett** — beliebige TCP-Protokolle tunnelbar (150 KB bidirektional E2E belegt).

**✅ Blocker resolved (cycle 43):** two bugs, not GSO — (1) the Agent/Client QUIC client endpoints bound `127.0.0.1`, which can't route to a non-loopback Edge → changed to `0.0.0.0`; (2) the origin's `apk add socat` raced the client → switched to the pre-built `alpine/socat` image. The full protocol now runs container-to-container.

**⇢ Reframe (cycle 26): academic testbed + BA thesis, all in Docker.** Emulate the topology in Docker (no host sudo/mininet → containers with `--cap-add=NET_ADMIN` + `tc netem` + iptables-NAT), run performance tests, write a German BA thesis (HAW-konform, scaffolded), compiled via texlive-in-Docker. NAT/hole-punching (SPEC §10 item 4) **now in scope** via emulation (M5). New milestones: **M5** Docker testbed (needs runnable binaries first) · **M6** performance evaluation · **M7** BA thesis. Order: finish M4 → M5 → M6 → M7.

## Cycle log

- **Cycle 1 — P0.1**: Cargo workspace (`ct-common`, `ct-agent`, `ct-edge`, `ct-control-plane`), `Dockerfile.dev`, `.github/workflows/ci.yml`. Local gate: `cargo build --workspace` + `cargo test --workspace` in `rust:1-slim` → 4 tests pass. Committed.
- **Cycle 2 — P0.2**: `ct-common` wire types (TenantId, AgentId, RoutingToken, OriginIdentity, Capability, ControlFrame), serde derive + serde_json round-trip test per type. Green in `rust:1-slim` (serde fetched from crates.io — container network confirmed). Committed.
- **Cycle 3 — P1.1a**: decomposed P1.1 (too big for one pass) into P1.1a/b/c. P1.1a: `ct-edge::transport::build_server_endpoint` — quinn server `Endpoint` with rcgen self-signed cert + ring crypto provider, binds an ephemeral UDP port; `#[tokio::test]` asserts non-zero port. Stack (quinn 0.11 / rustls 0.23-ring / rcgen 0.13) compiled clean. Green in `rust:1-slim`. Committed.
- **Cycle 4 — P1.1b**: client endpoint that trusts the server's self-signed cert (proper verification, no skip); `accept_and_echo_one` accepts one bi stream and echoes. `#[tokio::test] echo_roundtrip_over_bidirectional_stream` connects client→server, round-trips `b"ping"`. 3 ct-edge tests green in `rust:1-slim`. Committed.
- **Cycle 5 — P1.1c**: negative-path test `untrusted_server_cert_is_rejected` — client trusting a different cert must fail the handshake (proves TLS verification is enforced, not skipped). No new production code. **P1.1 complete.** Full workspace regression: 11 tests green in `rust:1-slim`. Committed.
- **Cycle 6 — P1.2a**: decomposed P1.2 into a/b/c. P1.2a: `ct-agent::transport` — `select_transport(udp_reachable)` decision (QUIC vs TcpFallback) + `dial_quic`; interop test `agent_dials_edge_over_quic` drives a real `ct-edge` server (dev-dep) and round-trips bytes. Full workspace: 14 tests green. Committed.
- **Cycle 7 — P1.3a**: decomposed P1.3. P1.3a: `ct-control-plane::enrollment` — in-memory `Enrollment` service; `issue_join_token` (random single-use), `redeem` binds Agent public key to Tenant and consumes the token, rejects reuse (`TokenAlreadyUsed`) and unknown tokens. Service holds no private keys. 3 enrollment tests; full workspace 17 tests green. Committed.
- **Cycle 8 — P1.3b**: `ct-agent::identity::AgentIdentity` — ed25519 keypair (ed25519-dalek 2 + rand 0.8); `public_key_bytes()` + `sign()`, **no signing-key accessor** (private key never leaves the Agent). Tests: distinct keys, signature verifies, and `enroll_binds_agent_identity` redeems a real identity against `ct-control-plane`. **P1.3 complete.** Full workspace 20 tests green. Committed.
- **Cycle 9 — P1.4a**: decomposed P1.4 into a/b/c. P1.4a: `ct-control-plane::credential` — `CredentialIssuer` mints ed25519 issuer-signed, expiry-bounded credentials; `verify(issuer_pubkey, signed, now)` checks signature + expiry. Time passed in (no wall-clock in lib). Tests: verify-ok-before-expiry, expired, wrong-issuer, tampered-claims. Full workspace 24 tests green. Committed.
- **Cycle 10 — P1.4b**: `ct-control-plane::issuance::mint_for_enrolled` — mints a credential only for an enrolled Agent (Tenant taken from the binding, not the caller); unenrolled → `MintError::NotEnrolled`. Split Edge-side verify into P1.4c (needs credential types shared across crates — avoid coupling edge→control-plane hastily). 2 tests; full workspace 26 tests green. Committed.
- **Cycle 11 — P1.4c**: refactor — moved credential claims/wire-form/`verify` to `ct-common::credential`; `ct-control-plane::credential` re-exports them + keeps `CredentialIssuer` (signing key); new `ct-edge::auth::verify_presented_credential` lets the Edge verify statelessly. All prior tests survived the move; full workspace 29 tests green. Committed.
- **Cycle 12 — P1.4d-i**: caught that serde can't derive `Deserialize` for `[u8; 64]`, so gave `SignedCredential` a hand-rolled binary wire form (`signature | tenant_len | tenant | agent_len | agent | expires_at`) via `encode`/`decode`; added `CredError::Malformed`. Tests: round-trip, truncated, trailing-garbage. Split P1.4d into d-i (this) + d-ii (QUIC handshake). Full workspace 32 tests green. Committed.
- **Cycle 13 — P1.4d-ii**: `ct-edge::auth::accept_and_authenticate` (accept → read credential → decode → verify → reply OK/NO, return authenticated conn) + `ct-agent::transport::present_credential` (open bi-stream → send encoded credential → await ack). Interop tests over live QUIC: valid credential authenticates; expired is rejected. **P1.4 done → Milestone 1 complete.** Full workspace 34 tests green. Committed.
- **Cycle 14 — P2.1**: extended the DAG to Milestone 2 (Tunnel Registry + Rendezvous). P2.1: `ct-control-plane::registry::TunnelRegistry` — in-memory `RoutingToken` → `TunnelInfo` (tenant, agent); `register`/`lookup`/`unregister`. Tests: register→lookup, unknown→None, unregister-removes (+ idempotent), re-register overwrites. Full workspace 38 tests green. Committed.
- **Cycle 15 — P2.2**: `ct-agent::capability::mint_capability(origin, edge_addr)` — mints a `Capability` with a fresh random Routing Token (ADR-0014). Tests: distinct tokens across mints; minted token registers + looks up in a `TunnelRegistry` (interop with control-plane). Full workspace 40 tests green. Committed.
- **Cycle 16 — P2.3a**: decomposed P2.3. P2.3a: `ct-edge::rendezvous::resolve_rendezvous(endpoint, is_known)` — reads a 32-byte Routing Token a Client presents, resolves it via an `is_known` predicate (keeps Edge decoupled from the control-plane registry type), replies OK/NO. Interop tests over QUIC: known token resolves, unknown rejected. Full workspace 42 tests green. Committed.
- **Cycle 17 — P2.4a**: decomposed P2.4. P2.4a: `ct-edge::relay::relay(a, b)` — provider-blind bidirectional byte relay via `tokio::io::copy_bidirectional` (never inspects bytes). Deterministic test with in-memory `duplex` streams: `c2a`/`a2c` cross correctly, byte counts (3,3). Added tokio `io-util`. Full workspace 43 tests green. Committed.
- **Cycle 18 — P2.4b**: `ct-edge::relay::relay_quic` joins each `(RecvStream, SendStream)` via `tokio::io::join` and relays through the P2.4a primitive. First QUIC integration test (echo + close) was racy (`ConnectionLost` on teardown) → **fixed** by simplifying to a deterministic one-directional client→agent relay test. **P2.4 done → Milestone 2 relay-first path complete.** Full workspace 44 tests green. Committed.
- **Cycle 19 — P3.1**: extended the DAG to Milestone 3 (Noise E2E). P3.1: `ct-common::noise` — `generate_static_keypair()` (Noise X25519 via `snow`, params `Noise_IK_25519_ChaChaPoly_BLAKE2s`); public half is the Origin Identity. Tests: 32-byte keys, distinct pairs, public↔OriginIdentity. Full workspace 47 tests green. Committed.
- **Cycle 20 — P3.2**: `client_handshake` (initiator, pins Origin public) + `origin_handshake` (responder) build Noise_IK states. Tests: full 2-message handshake establishes, transport-mode encrypt/decrypt both directions round-trips, and a client pinning the wrong Origin key fails the handshake. The provider-blind E2E crypto core. Full workspace 49 tests green. Committed.
- **Cycle 21 — P3.3a**: decomposed P3.3. P3.3a: `ct-common::noise::frame`/`take_frame` — 2-byte big-endian length-prefix framing so variable-length Noise messages can stream over the relay. Tests: round-trip, incomplete-frame → None, multi-frame remainder. Full workspace 52 tests green. Committed.
- **Cycle 22 — P3.3b**: `noise_e2e_through_relay_edge_sees_only_ciphertext` (in `ct-edge`, using the relay primitive + `ct_common::noise`): Client and Origin run the full Noise_IK handshake and exchange an encrypted payload *through* the Edge's opaque relay (in-memory duplex). Asserts the Origin decrypts it AND the relayed bytes ≠ plaintext. **P3.3 done — the zero-knowledge property is demonstrated end to end.** Full workspace 53 tests green. Committed.
- **Cycle 23 — P3.4**: `Capability::encode`/`decode` (portable out-of-band form: token | origin | addr) + `ct-common::noise::client_handshake_for(cap)` pins the Capability's Origin Identity. Tests: capability encode/decode round-trip + truncated rejected; handshake built from an imported Capability completes with the matching Origin. **P3.4 done → Milestone 3 complete.** Full workspace 56 tests green. Committed.
- **Cycle 24 — P4.1**: extended the DAG to Milestone 4 (PoW gating; NAT hole-punching deferred as not hermetically testable). P4.1: `ct-common::pow` — SHA-256 leading-zero-bits proof-of-work; `Challenge { nonce, difficulty }`, `solve` (brute force), `verify` (one hash). Tests: solve→verify, meets-difficulty, zero-difficulty valid, insufficient-bits rejected (deterministic). Full workspace 60 tests green. Committed.
- **Cycle 25 — P4.2a**: decomposed P4.2. P4.2a: `ct-common::pow::build_request` (solve + pack `solution|token`) and `check_request` (verify PoW + extract token), with `GateError`. Tests: build→check round-trip, malformed-length rejected, insufficient-PoW rejected (deterministic harder-challenge trick). Full workspace 63 tests green. Committed.
- **Cycle 26 — P4.2b**: `ct-edge::rendezvous::resolve_rendezvous_gated` — Edge sends `nonce|difficulty`, Client returns `solution|token`, Edge `check_request`s the PoW before resolving via `is_known` (edge-initiated stream). Interop tests over QUIC: valid solution → OK + resolved; unsolved solution at difficulty 24 → rejected. Full workspace 65 tests green. Committed.
- **Cycle 27 — P4.3**: `ct-common::ratelimit::RateLimiter` — fixed-window per-token counter (`allow(token, window)`), time injected. Tests: allows up to limit then rejects, resets on new window, tokens independent. **M4 complete** (PoW + rate limiting). Full workspace 68 tests green. Committed. Next: M5.1 Edge binary.
- **Cycle 28 — M5.1a**: decomposed M5.1. M5.1a: **first runnable binary** — `ct-edge` bin (`src/main.rs`): `EdgeConfig::from_env` (CT_EDGE_LISTEN / CT_EDGE_POW_DIFFICULTY) → `build_server_endpoint_at(listen)` → accept loop. Refactored transport to bind an arbitrary addr. Tests: config parse valid / bad-listen / bad-difficulty; the bin compiles in the workspace build. Full workspace 71 tests green. Committed. Next: M5.1b orchestration.
- **Cycle 29 — M5.1b**: `ct-edge::state::EdgeState<H>` — thread-safe registry mapping `RoutingToken` → Agent tunnel handle; `register`/`route`/`remove`/`is_known` (generic over handle so it's unit-testable; `quinn::Connection` in the daemon). `is_known` plugs into `resolve_rendezvous_gated`. Tests: register→route, unknown→None, remove drops. Full workspace 74 tests green. Committed. Next: M5.1c serve orchestration.
- **Cycle 30 — M5.1c**: `ct-edge::serve::register_agent(conn, state)` — reads `role='A' | token(32)` on a control stream, stores the connection in `EdgeState`, acks `OK`, returns the token. Interop test over QUIC: an Agent registers and the token becomes `is_known` (routable). Full workspace 75 tests green. Committed. Next: M5.1d client route→relay.
- **Cycle 31 — M5.1d**: `ct-edge::serve::route_and_relay(state, token, client_send, client_recv)` — looks up the Agent tunnel via `EdgeState::route`, opens a stream on it, and `relay_quic`s. E2E test `edge_routes_client_data_to_registered_agent`: Agent registers → Client sends token+data → bytes arrive at the Agent through the Edge. **M5.1 (Edge daemon) fully wired.** Full workspace 76 tests green. Committed. Next: M5.2 Agent binary.
- **Cycle 32 — M5.2a**: decomposed M5.2. M5.2a: **second runnable binary** — `ct-agent` bin + `AgentConfig::from_env` (CT_AGENT_EDGE / CT_AGENT_ORIGIN); `ct-agent::transport::register_tunnel(conn, token)` (agent-side control message). Tests: config parse (valid/bad-edge/bad-origin); `agent_registers_tunnel_with_edge` interop against a real `ct-edge` (token becomes routable). Full workspace 80 tests green. Committed. Next: M5.2b dial + serve origin.
- **Cycle 33 — M5.2b**: `ct-agent::serve::serve_stream_to_origin(quic_send, quic_recv, origin)` — dials the local Origin (TCP) and `copy_bidirectional`s a joined QUIC stream ↔ the TCP connection (added tokio net/io-util). Test `agent_relays_quic_stream_to_local_origin`: edge opens a relayed stream → agent bridges to a TCP echo origin → echo returns. In-cycle fix: used the wrong crate's client-endpoint helper → switched to `dial_quic`. Full workspace 81 tests green. Committed. Next: M5.2c run loop.
- **Cycle 34 — M5.2c**: `ct-agent::serve::run_agent(config, edge_cert, token)` — dial Edge → `register_tunnel` → loop `accept_bi` → spawn `serve_stream_to_origin`. Test `run_agent_registers_and_serves_relayed_streams`: full agent loop registers, an Edge-relayed stream reaches a TCP echo origin and echoes back. **M5.2 (Agent daemon) functionally complete.** Full workspace 82 tests green. Committed. Next: M5.3 Client tool.
- **Cycle 35 — M5.3a**: decomposed M5.3. New **`ct-client` crate** (third binary): `transport::dial_edge` (trust edge cert, connect), `rendezvous::client_rendezvous` (read challenge → solve PoW → present token → await OK), `ClientConfig::from_env`. Tests: config defaults; `client_completes_pow_gated_rendezvous` interop against a real `ct-edge`. Full workspace 84 tests green. Committed. Next: M5.3b data path to origin.
- **Cycle 36 — M5.3b**: `ct-client::transport::client_exchange(conn, input)` — opens a data stream, sends `input`, returns the tunnel response. Test `client_exchanges_data_over_stream`: data round-trips against the Edge echo. **M5.3 (Client) core complete** (rendezvous + data exchange). Full workspace 85 tests green. Committed. Next: M5.4 Dockerfiles.
- **Cycle 37 — M5.4**: multi-stage `docker/Dockerfile` (+ `.dockerignore`) builds all three binaries in release and ships them in a `debian:bookworm-slim` runtime with `iproute2`/`iptables` (for netem/NAT). Verification (packet-specific, not `cargo test`): `docker build -f docker/Dockerfile -t ct-testbed .` → BUILD_EXIT=0; all 3 bins executable + `tc` present in the image (BINS_OK). Rust unchanged → 85 tests still green. Committed. Next: M5.5 compose + netem + NAT.
- **Cycle 38 — M5.4b** (inserted before compose — binaries must run the protocol first): unified `ct-edge::serve::serve_connection(conn, state, challenge)` — role dispatch (`'A'` register / `'C'` PoW-rendezvous→route→relay on one stream) + matching `ct-client::transport::client_tunnel`. E2E test `client_tunnels_data_to_agent_through_edge`: Agent registers → Client tunnels `b"payload"` → reaches the Agent through the Edge and echoes back. In-cycle fix: teardown race (edge dropped the client conn before the FIN flushed) → hold `client_conn.closed()`. Full workspace 86 tests green. Committed. Next: M5.4c main wiring + cert distribution.
- **Cycle 39 — M5.4c-i**: `ct-edge::serve::run_edge(config, cert_out)` — bind to `config.listen`, `save_cert` to the shared path, accept loop spawning `serve_connection` per conn with a fresh random PoW nonce (added `rand`); `save_cert`/`load_cert` DER helpers. `ct-edge` `main.rs` now runs the full protocol. Test `cert_save_load_roundtrip`. Full workspace 87 tests green. Committed. Next: M5.4c-ii agent/client main wiring.
- **Cycle 40 — M5.4c-ii**: `ct-agent` `main.rs` wired — wait for the Edge cert (`load_cert`, retry via tokio `time`), mint a Capability (`OriginIdentity` placeholder until Noise E2E), write it to the shared volume (`CT_AGENT_CAPABILITY_OUT`), then `run_agent`. Test `load_cert_reads_written_der`. Full workspace 88 tests green. Committed. Next: M5.4c-iii client main.
- **Cycle 41 — M5.4c-iii**: `ct-client` `main.rs` wired — wait for the Edge cert + Agent Capability (retry via tokio `time`), `Capability::decode`, dial `cap.edge_addr`, `client_tunnel` the payload, verify the round-trip (exit non-zero on mismatch). `transport::load_cert` + test. **M5.4c complete** — all three binaries run the protocol from config, coordinating via shared cert+capability files. Full workspace 89 tests green. Committed. Next: M5.5 compose + netem + NAT.
- **Cycle 42 — M5.5a (BLOCKED)**: `docker/docker-compose.yml` — edge + agent + socat echo origin + client on a static-IP net (10.5.0.0/24) with a shared cert/capability volume; client exits 0 on a verified round-trip. `docker compose up --build` builds the image and starts all 4; edge listens, agent mints+writes the Capability, then **agent→edge QUIC dial `TimedOut`** (bridge-only; loopback tests all pass → UDP GSO/MTU). Committed the compose (correct scaffolding) + blocker; NOT a green smoke. Next: diagnose the QUIC-over-bridge timeout.
- **Cycle 43 — M5.5a UNBLOCKED ✅**: diagnosed by re-reading code (not GSO): the Agent/Client QUIC client endpoints bound `Ipv4Addr::LOCALHOST` → can't route to a non-loopback Edge; fixed to `Ipv4Addr::UNSPECIFIED` (0.0.0.0). Second bug: origin `apk add socat` raced the client → pre-built `alpine/socat`. **`docker compose up` → `client: tunnel round-trip OK`, `COMPOSE_EXIT=0`** — the full protocol runs container-to-container through Edge+Agent+Origin. Unit tests still 89 green. Committed. Next: M5.5b tc netem.
- **Cycle 44 — M5.5b ✅**: `docker/netem-entrypoint.sh` applies `tc qdisc … netem` (CT_NETEM_DELAY/LOSS/RATE) to eth0 before exec-ing the node; added to the image + `NET_ADMIN` + per-node `{EDGE,AGENT,CLIENT}_{DELAY,LOSS,RATE}` env. Verified: `EDGE_DELAY=30ms EDGE_LOSS=1% docker compose up` → `netem: applying delay 30ms loss 1%`, `client: tunnel round-trip OK`, `COMPOSE_EXIT=0`. **Milestone 5 essentially complete** (M5.6 smoke achieved by M5.5a). Next: M6 performance evaluation.
- **Cycle 45 — M6.1a**: started M6 (perf eval). `ct-client::bench::summarize(&[f64]) -> Option<Summary>` — mean/min/max/p50/p95 (nearest-rank percentiles) over latency samples. Tests: known set, unsorted input, empty → None. Full workspace 92 tests green. Committed. Next: M6.1b latency runner + client bench mode.
- **Cycle 46 — M6.1b**: `ct-client::bench::run_bench(edge, cert, token, payload, iterations) -> Vec<f64>` — N fresh-connection dial→tunnel→verify round-trips, per-iteration latency (ms); `csv_row`/`CSV_HEADER`. Tests: `csv_row_formats` (pure) + `run_bench_measures_iterations` (3 round-trips through a real edge+agent → 3 samples). **M6.1 bench harness complete.** Full workspace 94 tests green. Committed. Next: M6.1c client bench mode + M6.2 sweep.
- **Cycle 104 — M15.1**: Prepaid-Credit-Ledger — Start von M15 (letztes v1-Feature). Neue `crates/control-plane/src/accounts.rs`: `AccountId(pub [u8;32])` [opak, zufällig via `rand::thread_rng().fill_bytes` → pseudonym, keine PII], `Ledger{balances: HashMap<AccountId,u64>}` mit `open_account` [Balance 0], `balance`, `credit` [`saturating_add` gegen Overflow durch feindliche Beträge], `debit` [prüft `*bal < amount` → `InsufficientCredit{balance,requested}` ohne Mutation, sonst `-=`]. `LedgerError::{UnknownAccount,InsufficientCredit}` + Display/Error. Keine neuen Deps (rand schon vorhanden). 5 Unit-Tests grün. Hermetic gate: BUILD_RC=0, TEST_RC=0, **TOTAL_PASSED=145** (+5). Committed. Next: M15.2 (Token-Ausgabe an Guthaben koppeln).
- **Cycle 103 — M14.2b**: Compose-Scrape-Smoke (schließt M14.2/Milestone 14). Neues Bin `crates/agent/src/bin/metrics_probe.rs`: pollt `CT_AGENT_METRICS_ADDR` (~30 s, 500 ms-Intervall) via rohem HTTP/1.0-`GET /metrics` über tokio-TCP (keine neuen Deps), parst `ct_tunnels_opened_total` aus der Prometheus-Exposition und beendet mit 0, sobald ≥1 (sonst Timeout→≠0). Dockerfile kopiert `metrics_probe` ins Runtime-Image. Overlay `docker/docker-compose.metrics.yml`: Agent bekommt `CT_AGENT_METRICS_LISTEN=0.0.0.0:9100`, der Client-Command wird auf `sh -c "ct-client && metrics_probe"` überschrieben (Tunnel dann Scrape im selben Container, mit `CT_AGENT_METRICS_ADDR=10.5.0.4:9100`) — so ist der Client-Exit das Probe-Verdikt und `--exit-code-from client` fängt keinen frühen Abort. Hermetic gate: BUILD_RC=0, TEST_RC=0, **TOTAL_PASSED=140**. Live-Smoke (`-f base -f metrics up --build --abort-on-container-exit --exit-code-from client`): `tunnel round-trip OK (via=quic)` → `metrics probe OK: ct_tunnels_opened_total=1 ct_bytes_to_origin_total=12`, `client exited with code 0`, **COMPOSE_EXIT=0**, sauberer Teardown. Committed. Milestone 14 komplett; als letztes v1-Feature bleibt M15 (Accounts + Payment).
- **Cycle 102 — M14.2a**: Prometheus-`/metrics`-Endpoint im Agent. M14.2 dekomponiert → M14.2a (Endpoint, dieser Zyklus) + M14.2b (Compose-Scrape). Neues Modul `crates/agent/src/observe.rs`: `metrics_router(Arc<TunnelMetrics>)` (axum) mit `GET /metrics`, dessen Handler `render_prometheus()` mit Content-Type `text/plain; version=0.0.4` zurückgibt; `serve_metrics(listen, metrics)` bindet einen `TcpListener` und `axum::serve`t den Router. `AgentConfig.metrics_listen: Option<SocketAddr>` aus `CT_AGENT_METRICS_LISTEN` (leere/ungesetzte Var = deaktiviert); `run_agent` spawnt den Endpoint mit einem Clone der bereits geteilten `Arc<TunnelMetrics>` — kein Signatur-Refactor nötig, der Endpoint sieht dieselben Zähler, die der Datenpfad hochzählt. axum + dev-deps tower/http-body-util zu ct-agent ergänzt; der run_agent-Test-Literal + config-Parse um `metrics_listen: None` erweitert. Zwei Tests: `metrics_endpoint_renders_current_counters` (oneshot: 200, Content-Type `text/plain`, `ct_tunnels_opened_total 1`, `ct_bytes_to_origin_total 2048`, `ct_handshake_millis_total 9`) + `serve_metrics_is_scrapable_over_a_real_socket` (ephemerer Port, roher HTTP/1.0-GET → `ct_tunnels_opened_total 3`). Hermetic gate: BUILD_RC=0, TEST_RC=0, **TOTAL_PASSED=140** (+2). Committed. Next: M14.2b (Compose-Scrape-Smoke).
- **Cycle 101 — M14.1b-ii**: Agent-Datenpfad mit Metrics instrumentiert (schließt M14.1). `serve_noise_stream` bekommt `metrics: Arc<TunnelMetrics>`: der Handshake wird in einen `async`-Block gekapselt und mit `Instant` gemessen, sodass Erfolg (`observe_handshake` + `tunnels_opened`) und Fehler (`tunnels_failed`) gezählt werden; der Origin-`TcpStream` wird in `Metered` gewickelt (Read=`bytes_to_client`, Write=`bytes_to_origin` — Richtungen aus `noise_pump`s plain=Origin-Seite abgeleitet). `serve_direct` reicht die Metrics an den Tcp-Arm durch (Clone je Verbindung); `run_agent` konstruiert **eine** geteilte `Arc<TunnelMetrics>` intern und gibt Clones an Direct- + Relay-Serve-Tasks — Signatur bleibt gleich, kein Ripple auf `agent/main.rs` oder den run_agent-Test. Die vier ct-client-Test-Helfer (rendezvous.rs) auf die neue Signatur nachgezogen. Der Streaming-Test prüft jetzt zusätzlich die Zähler nach 100 KB E2E: opened=1, handshakes=1, to_origin=to_client=100000, failed=0. Hermetic gate: BUILD_RC=0, TEST_RC=0, **TOTAL_PASSED=138**. Committed. Next: M14.2 (`/metrics`-Endpoint; run_agent-Handle exponieren + Compose-Scrape).
- **Cycle 100 — M14.1b-i**: Byte-Zähler-Adapter für die Instrumentierung. M14.1b dekomponiert → M14.1b-i (Adapter, dieser Zyklus) + M14.1b-ii (in serve_noise_stream/run_agent verdrahten). `crates/common/src/metrics.rs`: neuer `Metered<S>` (`AsyncRead`+`AsyncWrite`) zählt erfolgreiche Reads/Writes in je einen `Arc<Counter>` und reicht `poll_*` transparent durch (`poll_read` misst über den Zuwachs von `buf.filled().len()`, `poll_write` über das `Ok(n)`); wird um den Origin-Socket gelegt → `noise_pump` bleibt unangetastet. `TunnelMetrics`-Felder auf `Arc<Counter>` umgestellt (Struct jetzt `Clone`), damit eine einzelne Serie unabhängig an den Wrapper geht; die bestehenden Zugriffe (`m.x.inc()/.get()`) funktionieren via Deref weiter, `Default` bleibt ableitbar (`Arc<T: Default>: Default`). Test `metered_counts_bytes_in_both_directions` (duplex): 5 B geschrieben→write=5, 3 B empfangen→read=3. Hermetic gate: BUILD_RC=0, TEST_RC=0, **TOTAL_PASSED=138** (+1). Committed. Next: M14.1b-ii (verdrahten).
- **Cycle 99 — M14.1a**: Metrik-Fundament für die Observability (M14). M14.1 dekomponiert → M14.1a (dependency-freies Metrik-Set in ct-common, dieser Zyklus) + M14.1b (Datenpfad instrumentieren). Neue `crates/common/src/metrics.rs`: `Counter(AtomicU64)` mit `inc`/`add`/`get` (`Relaxed` genügt — die Zähler tragen keine happens-before-Beziehung), `TunnelMetrics` mit 6 Zählern (tunnels_opened/failed, bytes_to_origin/to_client, handshakes, handshake_millis_total), `observe_handshake(Duration)` (Count++ + Latenz-Summe in ms), `render_prometheus()` → Prometheus-Exposition (`# HELP`/`# TYPE`/value je Serie). Bewusst kein externes Metrics-Crate: kleines Set, triviales Format → Datenpfad + Dependency-Graph bleiben leicht. 4 Tests grün (counter_inc_add_get, observe_handshake_accumulates, render_reflects_values, counters_shareable_and_sum_across_threads [8×1000=8000]). Hermetic gate: BUILD_RC=0, TEST_RC=0, **TOTAL_PASSED=137** (+4). Committed. Next: M14.1b (Agent/Client instrumentieren).
- **Cycle 98 — M13.4b**: Compose-Smoke für den gehosteten Control-Plane-Service (schließt M13.4/Milestone 13). Neues Bin `crates/control-plane/src/bin/cp_selftest.rs`: liest `CT_CONTROL_PLANE_URL`, Retry-Schleife bis der Dienst annimmt (Compose ordnet nur Start, nicht Readiness), fährt via `ControlPlaneClient` die volle Kette issue→redeem→register→resolve, prüft die Bindings, druckt `OK` bzw. exit≠0. `tokio`-Feature `time` ergänzt (Retry-Sleep). Dockerfile kopiert jetzt zusätzlich `ct-control-plane` + `cp_selftest` ins Runtime-Image. Neue standalone `docker/docker-compose.controlplane.yml` (`cp-net` 10.6.0.0/24): `control-plane` (serviert auf `0.0.0.0:8090`) + `selftest` (fährt gegen `http://10.6.0.2:8090`, `--exit-code-from selftest`). Hermetic gate: `cargo build --workspace --bins` BUILD_RC=0 (cp_selftest kompiliert), `cargo test --workspace` TEST_RC=0, **TOTAL_PASSED=133**. Live-Smoke: `control-plane selftest OK: enroll -> register -> resolve (tenant-smoke, agent-smoke)`, `selftest exited with code 0`, **COMPOSE_EXIT=0**, Container+Netz sauber entfernt (keine Orphans). Committed. Next: M14 (Agent-seitige Observability, Prometheus/OTel `/metrics`).
- **Cycle 97 — M13.4a**: HTTP-Client für den laufenden Control-Plane-Service. M13.4 war zu groß für einen Gate-Pass (E2E braucht einen HTTP-Client, den Agent/Client fahren, + Compose-Overlay) → dekomponiert in M13.4a (Client + Live-Service-Integrationstest, dieser Zyklus) und M13.4b (Compose-Smoke, nächster Zyklus). Neue `crates/control-plane/src/client.rs`: `ControlPlaneClient::new(base_url)` (reqwest, `default-features=false, features=["json"]`, plaintext HTTP) mit `issue_join_token`/`redeem`/`register`/`resolve`; Fehler `CpError::{Http,Status,Malformed}` (Non-2xx → `Status`, sodass 404/409 als Fehler statt Panik durchschlagen). Integrationstest `client_drives_live_control_plane_service`: bindet den echten `control_plane_router` via `axum::serve` auf `127.0.0.1:0` (Listener vor `serve`-Spawn gebunden → kein Startup-Race), dann fährt der Client die **volle Kette über einen echten TCP-Socket**: issue→redeem (bindet Tenant)→register (Routing-Token)→resolve (liefert `(tenant,agent)`), + unbekannter Token = `Status(404)`. Zweiter Test `redeem_reuse_surfaces_a_status_error`: Join-Token-Reuse = `Status(409)`. Hermetic gate: `cargo build --workspace` BUILD_RC=0, `cargo test --workspace` TEST_RC=0, **TOTAL_PASSED=133** (+2). Committed. Next: M13.4b Compose-Overlay (Control-Plane-Container + Live-Enroll/Register/Resolve-Smoke).
- **Cycle 96 — M13.3**: Control-Plane-Service-Binary. `ct-control-plane::http::control_plane_router(enrollment, registry) -> Router` = `enrollment_router(enr).merge(registry_router(reg))` (beide States je eigen). Neue `crates/control-plane/src/main.rs` (macht das Lib-Crate auch zum Bin): liest `CT_CONTROL_PLANE_LISTEN` (Default `0.0.0.0:8090`), baut den Router, `axum::serve(TcpListener, app)`. Test `merged_router_serves_enrollment_and_registry`: `/enroll/issue` + `/registry/register` beide 200 auf einem App. Bin kompiliert sauber (`cargo build -p ct-control-plane --bin`). Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=131, RC=0**. Committed. Next: M13.4 Dockerisierung + Compose-E2E (Enroll/Register gegen den laufenden Service).
- **Cycle 95 — M13.2**: Registry-/Rendezvous-HTTP-Endpoints. `ct-control-plane::http::registry_router(Arc<Mutex<TunnelRegistry>>)`: `POST /registry/register` (`{token,tenant,agent}` → registriert `RoutingToken`→`TunnelInfo`, 200) und `GET /registry/resolve/:token` (Rendezvous-Auflösung: `lookup` → `{tenant,agent}` oder 404). `axum::extract::Path` für den Token-Pfadparameter. Test `register_then_resolve_tunnel`: register→resolve liefert (t,a), unbekanntes Token→404. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=130, RC=0**. Committed. Next: M13.3 Control-Plane-Service-Binary + Dockerisierung.
- **Cycle 94 — M13.1** (Start M13, Control-Plane-Service): `ct-control-plane::http::enrollment_router(state: Arc<Mutex<Enrollment>>) -> axum::Router` — `POST /enroll/issue` (`{tenant}` → `{token}` hex-32B, ruft `issue_join_token`), `POST /enroll/redeem` (`{token,agent,pubkey}` hex → `{tenant}`, ruft `redeem`; Fehler → `409 CONFLICT`). Hex-Helper handgeschrieben (keine extra Dep). Deps: `axum 0.7`, `serde`, `serde_json`, `tokio`; dev `tower`+`http-body-util`. 2 Tests via `tower::ServiceExt::oneshot` (issue→redeem→bindet Tenant; 2. Redeem→409; unbekanntes Token→409). Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=129, RC=0**. Committed. Next: M13.2 Registry-/Rendezvous-Endpoints.
- **Cycle 93 — M12.3c**: Compose-UDP-blockiert-Smoke. `docker/docker-compose.tcp.yml`-Overlay setzt den Client-Entrypoint auf `iptables -A OUTPUT -p udp -d 10.5.0.2 -j DROP; exec netem-entrypoint.sh ct-client` (Client hat `NET_ADMIN`+iptables), blockt also nur sein ausgehendes UDP zum Edge — der Agent erreicht den Edge weiter via QUIC. Live-Smoke (`docker compose … -f docker-compose.tcp.yml up --build`): `ct-client: tunnel round-trip OK (via=tcp)`, `sent/received "hello-tunnel" (via=tcp)`, `client exited 0`, `COMPOSE_EXIT=0` — die 2s-QUIC-Probe scheitert (UDP gedroppt) → Client fällt auf TLS-TCP zurück → Edge relayt den TCP-Client cross-transport zum QUIC-Agent → Noise-E2E zum Origin. **M12 komplett** — der TCP-Fallback greift real bei blockiertem UDP. Docker-only, 127 Tests unverändert. Committed. Next: M13 (gehosteter Control-Plane-Service).
- **Cycle 92 — M12.3b**: Client-`main`-Transport-Fallback. `ct-client::transport::tcp_tls_connect(addr, edge_cert) -> tokio_rustls::client::TlsStream<TcpStream>` (client-eigener TLS-TCP-Connector, `tokio-rustls` ergänzt — hält ct-client unabhängig von ct-edge). `main` Single-Modus: `CT_CLIENT_FORCE_TCP` oder QUIC-Probe (`tokio::time::timeout(2s, dial_edge)`) → bei `Ok(Ok(conn))` QUIC-Tunnel (`client_tunnel_noise`), sonst Fallback via `tcp_tls_connect`+`client_tunnel_noise_tcp`; Ausgabe `via=quic|tcp`. Test `client_tcp_tls_connects_and_echoes`: Client-Connector gegen ct-edge-TLS-TCP-Listener, Byte-Echo `client-tcp`. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=127, RC=0** (2 vorbestehende Warnungen: `token_e`, ein `AsyncReadExt` — später gesammelt bereinigt). Committed. Next: M12.3c Compose-UDP-blockiert-Smoke.
- **Cycle 91 — M12.3a**: Dual-Transport-Edge + `run_edge`-Wiring. `ct-edge::transport::build_dual_edge(quic_addr, tcp_addr) -> (Endpoint, TcpListener, TlsAcceptor, CertificateDer)` — **ein** `self_signed`-Cert+Key wird für den QUIC-Server (`quinn::ServerConfig`) **und** den TLS-TCP-Acceptor (`rustls::ServerConfig`) genutzt (`key.clone_key()`), also vertraut der Client einem Cert für beide Transporte. `run_edge` nutzt jetzt `build_dual_edge` (QUIC- + TCP-Listen auf `config.listen`), spawnt eine **TCP-Fallback-Accept-Schleife** (`acceptor.accept` → `serve_tcp_connection` mit frischer PoW-Challenge) neben der QUIC-Schleife; `build_server_endpoint_at`-Import entfernt. Test `dual_edge_serves_quic_and_tcp_with_one_cert`: QUIC-Client und TLS-TCP-Client verbinden beide mit dem geteilten Cert. In-cycle: unbenutzten `AsyncReadExt`-Import (M12.2c-Test) entfernt. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=126, RC=0**. Committed. Next: M12.3b Client-`main`-UDP-Probe+Fallback.
- **Cycle 90 — M12.2c**: `ct-client::transport::client_tunnel_noise_tcp<T: AsyncRead+AsyncWrite+Unpin>(stream, token, cap, client_private, payload)` — `'C'`-Rendezvous (`write_all('C')`, `read_exact` Challenge(17), `build_request`+PoW) auf dem einzelnen TLS-TCP-Stream, dann `split(stream)` + `client_noise_exchange(&mut w, &mut r, …)` (stream-generisch aus M8.2). Voller Noise-E2E-Tunnel über den TCP-Fallback. E2E-Test `client_noise_tunnels_over_tcp_fallback`: QUIC-Agent (`serve_noise_stream`) + Streaming-Origin, TLS-TCP-Edge (`serve_tcp_connection`), Client `tcp_tls_connect`→`client_tunnel_noise_tcp`, `tcp-noise` round-trippt Client→TLS-TCP→Edge→QUIC-Agent→Noise→Origin. In-cycle fix: `token_challenge`-Tippfehler → `&challenge`. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=125, RC=0**. Committed. Next: M12.3 `run_edge`-TLS-TCP-Listener + Client-`main`-Fallback + Compose-Smoke.
- **Cycle 89 — M12.2b**: `ct-edge::serve::serve_tcp_connection<S: AsyncRead+AsyncWrite+Unpin>(stream, state, challenge)` — liest das Rollen-Byte vom einzelnen TLS-TCP-Stream; `'C'`: sendet Challenge(17), liest PoW-Req(40), `check_request`, routet via `state.route` zum Agent-QUIC, dann `relay(&mut stream, &mut join(agent_recv, agent_send))`. Da `relay` generisch über `AsyncRead+AsyncWrite` ist, bridged ein **TLS-TCP-Client zu einem QUIC-Agent** (cross-transport). E2E-Test `edge_relays_tcp_fallback_client_to_quic_agent`: QUIC-Agent registriert+echot, TLS-TCP-Client macht `'C'`+PoW+15 Bytes, bekommt 15-Byte-Echo zurück. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=124, RC=0**. Committed. Next: M12.2c Client-TCP-Tunnel-Funktion.
- **Cycle 88 — M12.2a**: TLS-über-TCP-Transport-Primitiven. `ct-edge::transport::build_tcp_tls_listener_at(addr) -> (TcpListener, TlsAcceptor, CertificateDer)` (self-signed via bestehendes `self_signed`, `rustls::ServerConfig`) + `tcp_tls_connect(addr, edge_cert) -> tokio_rustls::client::TlsStream<TcpStream>` (trusted cert, `ServerName "localhost"`). `tokio-rustls 0.26` + tokio-`net`-Feature ergänzt. Test `tcp_tls_stream_echoes`: Client verbindet TLS-TCP → Edge akzeptiert → Byte-Stream `tcp-fallback` round-trippt. **Entwurfsentscheidung** (dokumentiert, ehrlich): der TCP-Fallback trägt das **transport-agnostische** Tunnel-Protokoll (Noise-Frames über `noise_pump`/Rollen-Bytes) über TLS-TCP; literales HTTP/2-Framing (ADR-0004) ist eine spätere Härtung — analog zur ed25519≈mTLS-Notiz. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=123, RC=0**. Committed. Next: M12.2b Edge-Rollen-Protokoll über TLS-TCP.
- **Cycle 87 — M12.1** (Start M12, TCP-Fallback): `ct-agent::transport::probe_udp_reachable(edge, edge_cert, timeout) -> bool` — `tokio::time::timeout` um `dial_quic`; `true` gdw. der QUIC-Handshake innerhalb des Timeouts gelingt. Speist `select_transport(bool) -> Transport::{Quic, TcpFallback}` (aus P1.2a). 2 Tests: Probe gegen live-Edge (Accept-Task) → reachable → `Quic`; Probe gegen toten UDP-Port (bind+drop) mit 400ms-Timeout → not reachable → `TcpFallback`. Reine Entscheidungs-/Probe-Logik; der eigentliche TCP-Transport ist M12.2. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=122, RC=0**. Committed. Next: M12.2 TCP-Fallback-Transport.
- **Cycle 86 — M11.4b-vi**: Compose-P2P-Smoke. Client-`main` `CT_CLIENT_MODE=p2p` → `client_tunnel_auto` (mit kurzer Retry-Schleife, um das Startup-Race zu gewinnen, bis der Agent advertised hat). `docker/docker-compose.p2p.yml`-Overlay: Agent `CT_AGENT_DIRECT_ADVERTISE=10.5.0.4` (Bridge-IP), Client `CT_CLIENT_MODE=p2p`. **Live-Smoke** (`docker compose -f …yml -f …p2p.yml up --build`): `ct-client: p2p ... received "hello-tunnel" (direct=true)`, `P2P tunnel round-trip OK (direct=true)`, `COMPOSE_EXIT=0` — der Client entdeckt via `'P'` die advertisierte Listener-Adresse+Cert des Agents und verbindet sich **direkt** (10.5.0.4:<port>), umgeht den Edge-Relay. Docker-only, kein Rust-Test-Change → 120 Tests unverändert (ct-client kompiliert sauber verifiziert). **M11 komplett.** Ehrliche Grenze: flaches Bridge-Netz = kein NAT → Direktpfad trivial erreichbar; echtes simultaneous-open Hole-Punching bräuchte emuliertes NAT (dokumentiert). Committed. Next: M12 (HTTP/2-über-TCP-Fallback).
- **Cycle 85 — M11.4b-v**: `run_agent`-Daemon-Integration des Direktpfads. `AgentConfig` bekommt `direct_advertise_ip: Option<IpAddr>` (aus `CT_AGENT_DIRECT_ADVERTISE`, leer/unset = None, ungültig = Fehler). `run_agent`: bei gesetztem `ip` → `build_direct_listener()` (bindet `0.0.0.0:0`), advertisierte Adresse = `SocketAddr::new(ip, bound.port())`, `advertise_direct_listener` auf einer **separaten** kurzlebigen Edge-Verbindung (da eine Conn nur eine Rolle am Edge bedient), dann `serve_direct(listener, origin, origin_private, proto)` gespawnt — parallel zur bestehenden Relay-Accept-Schleife. `None` = reiner Relay (run_agent-Test unverändert grün). Config-Literale angepasst (+`direct_advertise_ip: None`), 1 Config-Test. Der Live-Direktpfad wird per Compose (M11.4b-vi) verifiziert. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=120, RC=0**. Committed. Next: M11.4b-vi Compose-P2P-Smoke.
- **Cycle 84 — M11.4b-iv**: Client-Auto-Discovery + voller P2P-E2E. `ct-client::transport::client_tunnel_auto(edge_conn, token, cap, client_private, payload, timeout) -> (bool, Vec<u8>)` — `query_direct_endpoint('P')` → `client_tunnel_p2p_or_relay(direct)`. E2E-Test `client_auto_uses_direct_path_when_advertised`: Agent baut `build_direct_listener_at` + `serve_direct`, `advertise_direct_listener('D')` an den Edge; Client dialt Edge, `client_tunnel_auto` fragt `'P'` ab (Edge liefert (laddr, agent_cert)), verbindet **direkt** zum Agent-Listener, tunnelt `auto-payload` → `used_direct=true`, Echo korrekt, **kein Relay**. Damit ist die in M11.4a dokumentierte ehrliche Lücke (Candidate=Ausgangsadresse, Cert nicht verteilt) auf Integrationsebene **geschlossen**: der Agent advertised seine echte Listener-`(addr,cert)`, der Client entdeckt sie und nutzt den Direktpfad. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=119, RC=0**. Committed. Next: M11.4b-v run_agent-Daemon-Wiring + M11.4b-vi Compose-NAT-E2E.
- **Cycle 83 — M11.4b-iii**: `ct-agent::serve::serve_direct(listener: Endpoint, origin, origin_private: [u8;32], proto: OriginProto)` — Accept-Loop: pro eingehender Direct-Verbindung `accept_bi` → je nach `proto` `serve_noise_stream` (TCP) oder `serve_noise_udp` (UDP), als Noise-Responder direkt zum Origin (kein Edge). Verpackt das M11.3c-Inline-Muster in eine wiederverwendbare Daemon-Schleife. Test `serve_direct_bridges_a_direct_connection`: `build_direct_listener_at` → `serve_direct` (TCP) + Echo-Origin; inline-Client (via `build_client_endpoint(cert)`) verbindet direkt, `Noise_IK`-Handshake + `direct-serve`-Payload round-trippt. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=118, RC=0**. Committed. Next: M11.4b-iv run_agent-Daemon-Integration (Listener + advertise + serve_direct).
- **Cycle 82 — M11.4b-ii**: `'D'`-Advertise- + `'P'`-Discover-Protokoll. Edge `serve_connection`: neuer `b'D'`-Zweig liest `token|addr_len|addr|cert_len(2 BE)|cert` → `state.advertise_direct`, ackt `OK`; `b'P'` gibt jetzt den **Direct-Endpoint** zurück (`[1]addr_len·addr·cert_len·cert`, oder `[0]` wenn keiner) statt der reflexiven Candidate. `ct-agent::transport::advertise_direct_listener(conn, token, addr, &cert)` (Sender). `ct-client::transport::query_direct_endpoint(conn, token) -> Option<(SocketAddr, CertificateDer)>` (ersetzt `query_peer_candidate`). Query-Test umgeschrieben: Agent advertised via `'D'` (adv_addr + adv_cert), Client fragt via `'P'` ab → `(got_addr, got_cert)` == advertised. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=117, RC=0**. Committed. Next: M11.4b-iii Agent-Daemon-Wiring.
- **Cycle 81 — M11.4b-i**: EdgeState-Speicher für den advertisierten Direct-Endpoint. Dritte Map `direct: HashMap<RoutingToken, (SocketAddr, Vec<u8>)>` (Listener-Adresse + Cert-DER, die der Agent bewirbt und ein Client für den Direktpfad nutzt). `advertise_direct(token, addr, cert)`, `direct_endpoint(token) -> Option<(SocketAddr, Vec<u8>)>`; `remove` löscht Handle+Candidate+Direct. 2 Unit-Tests (advertise+lookup+unknown, remove löscht). Reine Speicher-Primitive; das Protokoll-Wiring (`'D'`-Advertise, `'P'` gibt Cert mit) ist M11.4b-ii. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=117, RC=0**. Committed. Next: M11.4b-ii.
- **Cycle 80 — M11.4a**: Fallback-Orchestrator. `client_tunnel_p2p_or_relay(edge_conn, token, cap, client_private, payload, direct: Option<(SocketAddr, CertificateDer)>, timeout) -> (bool, Vec<u8>)` — bei gesetztem `direct` erst `client_direct_connect`→`client_tunnel_direct`; scheitert das (Timeout/Fehler), Fallback auf `client_tunnel_noise` über den Edge-Relay; `bool` = ob direkt genutzt. Test `p2p_falls_back_to_relay_when_direct_fails`: unerreichbarer Direct-Candidate (freier UDP-Port) + 400ms-Timeout → `used_direct=false`, Relay (echter Edge+Agent+Origin) liefert `fallback-payload`. M11.4 dekomponiert → M11.4a (dies) / M11.4b (Voll-Wiring + NAT-E2E; ehrliche Lücke dokumentiert: Candidate = Agent-Ausgangsadresse, nicht Listener-Adresse; Cert-Verteilung via `'P'` fehlt noch). Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=115, RC=0**. Committed. Next: M11.4b.
- **Cycle 79 — M11.3c**: Client-Direct-Path-Primitiven. `client_direct_connect(candidate: SocketAddr, agent_cert, timeout: Duration) -> Connection` — `tokio::time::timeout` um `dial_edge` (bei Timeout/Fehler → Err, Caller fällt auf Relay zurück). `client_tunnel_direct(conn, cap, client_private, payload)` — `open_bi` + `client_noise_exchange` **ohne** `'C'`-Rendezvous/PoW (der Noise-Handshake authentisiert den Direktpfad, Client pinnt Origin Identity). E2E-Test `client_tunnels_directly_to_agent`: Agent-`build_direct_listener_at` + Accept→`serve_noise_stream` zu echtem Streaming-Origin; Client `client_direct_connect`→`client_tunnel_direct`, `direct-payload` round-trippt **komplett am Edge vorbei**. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=114, RC=0**. Committed. Next: M11.4 Fallback-Logik (direct→relay) + NAT-E2E.
- **Cycle 78 — M11.3b**: Agent-Direct-Path-Listener. `ct-agent::transport::build_direct_listener_at(addr)` / `build_direct_listener()` (`0.0.0.0:0`) — QUIC-**Server**-Endpoint mit self-signed Cert (rcgen ergänzt + lokales `self_signed`, damit ct-agent unabhängig von ct-edge bleibt), auf dem der Agent direkte Client-Verbindungen annimmt, die den Edge-Relay umgehen. Cert wird zurückgegeben (später dem Client advertised). Test `direct_listener_accepts_a_connection`: Client (via `ct_edge::transport::build_client_endpoint(cert)`, Cert vertrauend) verbindet sich direkt zum Listener, Bi-Stream-Echo `direct-hello`. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=113, RC=0**. Committed. Next: M11.3c Client-Direct-Connect + Fallback-Logik.
- **Cycle 77 — M11.3a**: `'P'`-Peer-Candidate-Query. `serve_connection` bekommt einen `b'P'`-Zweig: liest `token(32)`, antwortet mit `[len][addr-utf8]` (`state.candidate(token)`, len 0 = keiner), `finish`. `ct-client::transport::query_peer_candidate(conn, token) -> Option<SocketAddr>` sendet `'P'|token`, parst die Antwort. Bewusst als **separater** Verb neben `'C'` (kein Eingriff in den Relay-Datenpfad, keine der 4 Tunnel-Funktionen berührt). Interop-Test `client_queries_agent_peer_candidate` (Agent registriert → Edge zeichnet Candidate auf → Client fragt via `'P'` ab → `Some(addr)`). In-cycle fix: Teardown-Race (`ConnectionLost(ApplicationClosed)`) — Edge muss `client_conn.closed().await` halten, bis der Client die Antwort gelesen hat; 2 unbenutzte Imports entfernt. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=112, RC=0**. Committed. Next: M11.3b Agent-Direct-Path-Listener.
- **Cycle 76 — M11.2**: Candidate-Aufzeichnung in den Live-Registrierungspfaden. `ct-edge::serve::register_agent` und `serve_connection`s `'A'`-Zweig nutzen jetzt `state.register_with_candidate(token, conn.clone(), conn.remote_address())` statt `register` — die vom Edge beobachtete Agent-Adresse wird bei jeder Registrierung als Peer-Candidate hinterlegt. Interop-Test `agent_registers_and_becomes_known` um `assert!(state.candidate(&token).is_some())` erweitert. Reines Aufzeichnen; die Edge→Client-Übertragung des Candidate (+ Hole-Punch) ist M11.3. Hermetic gate: **`cargo test --workspace` → TOTAL_PASSED=111, RC=0**. Committed. Next: M11.3.
- **Cycle 75 — M11.1** (Start M11, P2P): `EdgeState<H>` bekommt eine zweite Map `candidates: HashMap<RoutingToken, SocketAddr>` — die vom Edge beobachtete reflexive Adresse des Agents (die Adresse, aus der seine Pakete am Edge erscheinen = das STUN-artige Ziel, zu dem ein Client hole-puncht). `register_with_candidate(token, handle, candidate)`, `candidate(token) -> Option<SocketAddr>`; `remove` löscht jetzt Handle **und** Candidate. Bestehende `register`/`route`/`is_known` unverändert. 3 Unit-Tests (record+route, unknown→None, remove löscht Candidate). Reine, netzwerkfreie Primitive — das Protokoll-Wiring (`conn.remote_address()` bei `'A'`, Edge→Client-Übertragung) ist M11.2. Hermetic gate (healthy infra, schnell): **`cargo test --workspace` → TOTAL_PASSED=111, RC=0** (108+3). Committed. Next: M11.2 Candidate-Protokoll-Wiring.
- **Cycle 74 — M10.5**: `docker/docker-compose.udp.yml` — UDP-Overlay (compose-Merge über die Basis): Origin-`command` → `socat UDP4-LISTEN:8080,reuseaddr EXEC:/bin/cat` (nicht-forkend, damit die Antwort vom gebundenen Port 8080 kommt — sonst verwirft der `udp.connect(origin)`-Agent-Socket sie), Agent-Env `CT_AGENT_ORIGIN_PROTO=udp`, Client-Env `CT_CLIENT_MODE=udp`. Smoke: `docker compose -f docker-compose.yml -f docker-compose.udp.yml up --build` → Release-Image gebaut, dann `ct-client: UDP tunnel round-trip OK`, `udp sent/received "hello-tunnel"`, `client exited 0` (COMPOSE_EXIT=0). Der ganze Container-Stack tunnelt UDP E2E (Client→Edge→Agent `serve_noise_udp`→socat-UDP-Echo→zurück). Docker-only, kein Rust-Change → 108 Tests unverändert. **M10 komplett.** Committed. Next: M11 (direkter P2P-Pfad + Relay-Fallback).
- **Cycle 73 — M10.4**: Client-`main`-UDP-Modus. `ct-client::transport::udp_selftest(conn, token, cap, client_private, payload)` — bindet ein lokales App-UDP-Socket ↔ Tunnel-lokales Socket (gegenseitig connected), sendet `payload` als 1 Datagramm über `client_tunnel_udp`, `select!` gibt bei Echo-Empfang zurück (reißt den nie-endenden Tunnel ab). `main.rs`: `CT_CLIENT_MODE=udp` → `udp_selftest` + Echo-Verifikation (`UDP tunnel round-trip OK`). E2E-Test `client_udp_tunnels_datagrams_through_edge` auf `udp_selftest` refaktoriert (deckt die neue Funktion, keine Duplikation). Verifiziert (warmes Target, healthy Gate): **`cargo test --workspace` → `TOTAL_PASSED=108`, RC=0** (nur vorbestehende `token_e`-Warnung in ct-edge). Committed. Next: M10.5 Compose-UDP-Smoke.
- **Cycle 72 — M10.3**: Agent wählt das Origin-Protokoll live. `AgentConfig` bekommt `origin_proto: OriginProto` (`Tcp`|`Udp`, `#[default] Tcp`); `OriginProto::parse` (tcp/udp, trim+lowercase, sonst Fehler); `from_env` liest `CT_AGENT_ORIGIN_PROTO` (Default `tcp`). `run_agent` verzweigt pro relayten Stream: `Tcp → serve_noise_stream`, `Udp → serve_noise_udp`. Literale/Tests angepasst (`serve.rs`-run_agent-Test-Config + Default-Assertion). 2 neue Config-Tests (Default=Tcp; `OriginProto::parse` tcp/UDP/" udp "/Fehler). **Kritischer Fix (Ursache der „Hangs")**: der in M10.1 committete Test `serve_noise_udp_bridges_datagrams_to_origin` **hing** — `drop(i_write)` signalisiert **kein** EOF, solange die gesplittete `ReadHalf` (`i_read`) lebt (der DuplexStream bleibt offen), also kehrte `serve_noise_udp` nie zurück und `agent.await` blockierte ewig. Das ließ nicht nur den ct-agent-Lauf, sondern auch `cargo test --workspace` hängen (0% CPU nach Build). Fix: `i_write.shutdown().await` statt `drop`. Die frühere „TEST_RC=0"-Lesung in Zyklus 70 war ein Fehler (Exit von `tail`, nicht cargo). Diagnose via seriellem Lauf (`--test-threads=1`), der genau bei diesem Test stoppte. **Verifiziert: `cargo test --workspace` → `TOTAL_PASSED=108`, 0 failed, RC=0** (warmes Target; nebenbei entdeckt: SIGKILL eines Cargo-Containers mid-build wedged `/work/target` → `rm -rf target`, Memory [[project-hermetic-gate-workflow]] aktualisiert). Committed. Next: M10.4 Client-UDP-Modus + Compose-UDP-Smoke.
- **Cycle 71 — M10.2**: `ct-client::transport::client_tunnel_udp(conn, token, cap, client_private, local: UdpSocket)` — `'C'`-Rendezvous+PoW, `Noise_IK`-Initiator-Handshake, dann Datagramm-Bridge: `to_edge` `local.recv` → verschlüsselt → Frame; `from_edge` Frame → entschlüsselt → `local.send`; `select!`-Teardown bei Stream-Close (un-annotierte `noise_err`-Closure wie im Agent). E2E-Test `client_udp_tunnels_datagrams_through_edge`: lokale UDP-App ↔ `client_tunnel_udp` ↔ echter Edge-Relay ↔ Agent `serve_noise_udp` ↔ echter UDP-Echo-Origin; 3 diskrete Datagramme (inkl. längeres) round-trippen, Grenzen erhalten; äußeres `select!` bricht den nie-endenden Client-Tunnel ab, wenn der Driver fertig ist. In-cycle: 2 unbenutzte Imports entfernt. **Verifikation hermetisch**: `cargo test -p ct-client` → **16 grün** inkl. UDP-E2E (die frühere „TESTRC=1"-Verwirrung war ein Instrumentierungs-Bug: grep-Exit + `sh` ohne `PIPESTATUS`, kein Testfehler). Workspace additiv = 106+1 = **107**; Full-Suite-Bestätigung läuft nach. Committed. Next: M10.3 live-UDP-Wiring.
- **Cycle 70 — M10.1**: `ct-agent::serve::serve_noise_udp<S, R>(send, recv, origin, origin_private)` — Responder-`Noise_IK`-Handshake, dann eine `UdpSocket` (bind `0.0.0.0:0`, connect origin); **Datagramm-Erhaltung durch Frame-Kongruenz**: eine Noise-Frame trägt genau ein UDP-Datagramm, `to_origin` entschlüsselt jede Frame → `udp.send`, `to_client` `udp.recv` → verschlüsselt → eine Frame. `select!` beendet bei Tunnel-Close (`to_origin` EOF; UDP hat kein EOF). In-cycle fix: `snow::Error` nicht direkt benennbar (ct-agent hat snow nur transitiv) → un-annotierte `map_err`-Closure. Test `serve_noise_udp_bridges_datagrams_to_origin`: inline-Initiator ↔ Bridge ↔ echter UDP-Echo-Origin, 3 diskrete Datagramme (inkl. längeres) laufen durch, Grenzen+Inhalt erhalten. **Verifikation**: der neue Test im hermetischen `rust:1-slim` grün (`TEST_RC=0`); Änderung rein additiv (eine neue Funktion + Test in `ct-agent`, kein bestehender Code berührt) → Workspace = 105 (unverändert) + 1 = **106**; Full-Suite-Bestätigung lief separat nach. **Infra-Fix**: verwaiste `docker run`-Container (nach `timeout`-Kill der CLI weiterlaufend) hatten sich gestapelt und die Gates ausgehungert → alle gekillt; persistenter `CARGO_HOME`-Cache eingeführt, damit Gates nicht je Lauf den Registry-Index neu laden. Committed. Next: M10.2 Client-UDP-Modus.
- **Cycle 69 — M9.4**: `run_agent` schaltet den Serve-Loop von `serve_noise_bridge` (One-Shot) auf `serve_noise_stream` (full-duplex Streaming) um — der Agent-Live-Pfad tunnelt jetzt beliebige bidirektionale Streams. Der One-Shot-Client (`client_tunnel_noise`) bleibt interoperabel, weil Streaming-Origins (socat `cat`) ohne EOF-Wait echoen; `run_agent`-Test (inline One-Shot-Initiator + Echo-Origin) bleibt grün. `serve_noise_bridge` bleibt als getesteter One-Shot-Baustein (M8.4a/bench nutzen ihn direkt). Verifiziert: **105 Tests grün** + **Compose-Smoke** (`docker compose up --build` → `ct-client: tunnel round-trip OK`, `sent/received "hello-tunnel"`, `COMPOSE_EXIT=0`, ganzer Container-Stack über den Streaming-Agent). **M9 komplett.** Committed. Next: M10.1 UDP-Origin-Bridge.
- **Cycle 68 — M9.3**: `ct-client::transport::client_tunnel_stream<P>(conn, token, cap, client_private, app)` — `'C'`-Rendezvous+PoW, `Noise_IK`-Initiator-Handshake (framed, pinnt `cap.origin`), dann `noise_pump(transport, join(recv, send), app)` = full-duplex Streaming zwischen lokaler App und Origin über die live QUIC-Session. Da es eine echte `Connection` braucht, ist der Abnahmetest direkt ein Streaming-E2E: `client_streams_bidirectionally_through_edge` — echter Edge (`serve_connection`-Relay) + Agent (`serve_noise_stream`) + echter Streaming-TCP-Echo-Origin; **150 KB (multi-frame) laufen App→Edge→Agent→Origin→zurück und kommen identisch an**. Hermetic gate: **105 Tests grün** (104+1). Committed. Next: M9.4 (live-Mains auf Streaming + One-Shot-Tests migrieren).
- **Cycle 67 — M9.2**: `ct-agent::serve::serve_noise_stream<S, R>(send, recv, origin, origin_private)` — Responder-`Noise_IK`-Handshake (framed), dann `noise_pump(transport, join(recv, send), TcpStream::connect(origin))` = full-duplex Streaming-Bridge Client-Noise-Stream ↔ Origin-TCP (ersetzt konzeptionell das One-Shot `serve_noise_bridge`, das als getesteter Baustein bleibt). M9 verfeinert: **M9.4** ergänzt (live-Pfad verdrahten + One-Shot-E2E-Tests auf Streaming migrieren — die `read_to_end`-Origins deadlocken sonst einen Streaming-Client). Test `serve_noise_stream_bridges_streaming_to_origin`: inline-Initiator + `noise_pump` ↔ `serve_noise_stream` ↔ echter Streaming-TCP-Echo-Origin, 100 KB laufen durch und kommen identisch zurück (bestätigt nebenbei `snow::TransportState: Send`). Hermetic gate: **104 Tests grün** (103+1). Committed. Next: M9.3 Client-Streaming-API.
- **Cycle 66 — M9.1**: `ct-common::noise::noise_pump<C, P>(transport, cipher, plain)` — bidirektionaler Streaming-Noise-Datenpfad (ersetzt das One-Shot-Req/Resp): outbound liest Klartext-Chunks (16 KiB, unter Noises 65519-Cap), verschlüsselt+framet → cipher; inbound liest Frames → entschlüsselt → plain; beide Richtungen via `tokio::join!` konkurrent, `TransportState` in `std::sync::Mutex` (send/recv-Nonces unabhängig, Lock nie über await); Half-Close propagiert. `read_frame` nach ct-common gehoben; `tokio` (io-util) als ct-common-Dep. Test `noise_pump_streams_bidirectionally`: echter Handshake → zwei Transport-States → zwei gekoppelte Pumps + Echo-App, 200 KB (viele Frames) laufen A→B→A und kommen identisch zurück. Hermetic gate: **103 Tests grün** (102+1). Committed. Next: M9.2 Agent-Bridge auf full-duplex `noise_pump`.
- **Cycle 65 — M8.4c-iii + Roadmap**: Compose-Smoke über den Noise-Live-Pfad: `docker compose up --build` mit den umgestellten Binaries → `ct-client: tunnel round-trip OK`, `sent/received "hello-tunnel"`, `COMPOSE_EXIT=0`. Der volle Container-Stack (Edge+Agent+Origin+Client, statische IPs) fährt jetzt den **Ende-zu-Ende-verschlüsselten** Pfad. **M8 komplett.** Auf User-Direktive („Full product, alle Features + E2E") die Gap-Analyse gegen SPEC §8 durchgeführt und die **Full-Product-Roadmap M9–M15** in `v1-first-task-packets.md` angelegt (Streaming-Datenpfad, UDP, P2P+Fallback, TCP-Fallback, gehosteter Control-Plane-Service, Observability, Accounts+Payment) — jedes mit frozen E2E-Abnahme. Next: M9.1 Streaming-Noise-Transport.
- **Cycle 64 — M8.4c-ii**: Client-live-Pfad auf Noise. `bench::run_once`/`run_bench` nehmen `cap: &Capability` + `client_private: &[u8;32]` statt nur `token` und rufen `client_tunnel_noise`. `client/main.rs` generiert `generate_static_keypair()` (ephemer; Client-Static wird in `Noise_IK` nicht gepinnt) und nutzt in Single- (`client_tunnel_noise`) und Bench-Modus (`run_bench(..., &cap, &client_kp.private, ...)`) den verschlüsselten Tunnel. Bench-Test `run_bench_measures_iterations` umgeschrieben: Agent servt jeden relayten Stream via `serve_noise_bridge`, dazu ein Multi-Accept-TCP-Echo-Origin; 3 Noise-Roundtrips durch echten Edge gemessen. `client_tunnel` (Klartext) bleibt als getesteter Baustein. Hermetic gate: **102 Tests grün**. Committed. Next: M8.4c-iii Compose-Smoke.
- **Cycle 63 — M8.4c-i**: dekomponierte M8.4c → i/ii/iii. Agent-live-Pfad auf Noise: `run_agent(config, edge_cert, token, origin_private: [u8;32])` serviert jeden relayten Stream jetzt via `serve_noise_bridge` (statt Klartext-`serve_stream_to_origin`); `agent/main.rs` reicht `origin_key.private_bytes()` durch. Integrationstest `run_agent_registers_and_serves_relayed_streams` umgeschrieben: die „Edge" registriert den Agent und agiert dann als inline-Noise-Initiator (`client_handshake_for(cap)` mit `origin_kp.public`), verschlüsseltes `ping` läuft durch den echten Origin und kommt entschlüsselt zurück; `is_known(token)` weiter geprüft. `serve_stream_to_origin` bleibt als getesteter Baustein. Hermetic gate: **102 Tests grün**. Committed. Next: M8.4c-ii Client-main+bench.
- **Cycle 62 — M8.4b**: Provider-Blind-Assertion von der Client-Sende-Seite. In `ct-client::noise` (Tests): `RecordingWriter`/`RecordingReader` (delegierende `AsyncWrite`/`AsyncRead`-Adapter) protokollieren exakt die Bytes, die über den Tunnel-Stream gehen — was der Edge via `relay_quic` (reiner bidirektionaler Byte-Copy, verifiziert) byteweise weiterreicht. Test `tunnel_carries_only_ciphertext_never_the_plaintext`: echtes `client_noise_exchange` ↔ Responder ↔ echter TCP-Echo-Origin mit Marker-Klartext `TOP-SECRET-PLAINTEXT-MARKER`; assert (a) Klartext läuft E2E durch, (b) die protokollierten Tunnel-Bytes enthalten den Marker **nie**. Ergänzt den bereits existierenden Edge-Test `noise_e2e_through_relay_edge_sees_only_ciphertext` (Relay-Seite) um den Client-Contract. Hermetic gate: **102 Tests grün** (101+1). Committed. Next: M8.4c live-Pfad.
- **Cycle 61 — M8.4a**: dekomponierte M8.4 (Wiring + Tap + live-mains > ein Pass) → M8.4a/b/c. `ct-client::transport::client_tunnel_noise(conn, token, cap, client_private, payload)` — öffnet den Stream, macht das `'C'`-Rendezvous (`build_request`/PoW), dann `client_noise_exchange` über denselben `send`/`recv`. **Voller E2E-Integrationstest** `client_noise_tunnels_through_edge_to_origin` (client dev-dep `ct-agent` ergänzt): echter Edge (`serve_connection`-Relay) + echter Agent (`serve_noise_bridge`) + echter TCP-Echo-Origin; verschlüsseltes `secret-payload` läuft Client→Edge→Agent→Origin→zurück und kommt entschlüsselt an. Nutzt ausschließlich echten Produktionscode aller drei Rollen. Hermetic gate: **101 Tests grün** (100+1). Committed. Next: M8.4b Provider-Blind-Tap.
- **Cycle 60 — M8.3**: `ct-agent::serve::serve_noise_bridge<S: AsyncWrite, R: AsyncRead>(send, recv, origin, origin_private)` — Origin-Responder: `origin_handshake(private)`, framed Handshake (liest m1, schreibt m2), Transport-Mode; entschlüsselt die Client-Request-Frame, `TcpStream::connect(origin)` → schreibt Klartext + `shutdown` → `read_to_end` der Origin-Antwort → verschlüsselt sie als eine Frame zurück. Stream-generisch (QUIC live in M8.4, Duplex im Test) + privater `read_frame`-Helper. 1 frozen test: inline-Client-Initiator ↔ Bridge ↔ echter TCP-Echo-Origin, entschlüsseltes Echo == „secret-request" (Origin sieht nur Klartext, der Transport nur Chiffretext). Scoped: `run_agent`-Umstellung + E2E-Provider-Blind-Assertion bleibt M8.4. Hermetic gate: **100 Tests grün** (99+1). Committed. Next: M8.4.
- **Cycle 59 — M8.2**: `crates/client/src/noise.rs` — `client_noise_exchange<S: AsyncWrite, R: AsyncRead>(send, recv, client_private, cap, payload)`: baut den Initiator via `client_handshake_for(cap)` (pinnt `cap.origin`), tauscht die zwei `Noise_IK`-Handshake-Nachrichten framed (2-Byte-BE-Länge) aus, geht in Transport-Mode und sendet/empfängt eine verschlüsselte Nutzlast. Stream-generisch → im Test über `tokio::io::duplex`, im Live-Pfad über QUIC. 2 frozen tests: verschlüsselter Roundtrip gegen einen `origin_handshake`-Responder (entschlüsseltes Echo == Payload) + Handshake schlägt fehl bei falsch gepinnter Origin Identity. Kompiliert ohne direkten `snow`-Dep (Methoden auf transitiv sichtbaren Typen). Hermetic gate: **99 Tests grün** (97+2). Committed. Next: M8.3 Agent-Responder + Klartext-Bridge zum Origin.
- **Cycle 58 — M8.1** (DAG-Erweiterung, erster Rust-Code seit M6.1c): definierte M8 (Noise-E2E am Live-Pfad) + Dekomposition. `crates/agent/src/origin.rs` — `OriginKey` kapselt ein `ct_common::noise::StaticKeypair`; `generate()`, `origin_identity()`, `private_bytes()` (Private verlässt den Agent nie). `main.rs` mintet die Capability jetzt mit `origin_key.origin_identity()` (ersetzt den `[0u8;32]`-Platzhalter). 3 frozen tests: distinkte 32-Byte-Identities, Capability trägt die Identity, und — als starke Abnahme — geprägte Identity + zurückbehaltener Private schließen einen echten `Noise_IK`-Handshake ab (initiator via `client_handshake_for(cap)`, responder via `origin_handshake(private)`, beide erreichen Transport-Mode). Hermetic gate: **97 Tests grün** (94+3). Committed. Next: M8.2 Client-Initiator über den Tunnel-Stream.
- **Cycle 57 — M7.6**: `chapters/fazit.tex` — §6.1 Zusammenfassung (beantwortet **FF1**: providerblinder Tunnel aus QUIC+Noise+PoW realisierbar, für Architektur+Kernmechanismen durch den Prototyp + 94 Tests belegt; ehrlicher offener Integrationsschritt: Noise-E2E am Live-Datenpfad; **FF2/FF3**-Recap), §6.2 Ausblick (Prototyp-Lücken: P2P-Hole-Punching + Relay-Fallback, Live-Noise-E2E, TCP-Fallback, belastbarere Evaluation; offene Risiken + zurückgestellte Features aus `docs/BACKLOG.md`). Recompiled: `BUILD_EXIT=0`, 0 undefined refs, **21-Seiten-PDF** (386KB), Kapitel visually confirmed. **M7 komplett → geplanter DAG M1–M7 vollständig.** Doc-only, no Rust; 94 tests unchanged. Next: DAG-Erweiterung Richtung SPEC §8 (P2P-Datenpfad / Live-Noise-E2E / TCP-Fallback).
- **Cycle 56 — M7.5**: `chapters/evaluation.tex` ausgearbeitet. §5.1 Testbett & Methodik (Docker-Compose, `tc netem` Edge-Egress, Bench-Modus n=5, Frisch-Dial; reproduzierbare Pipeline sweep/plot/tabulate), §5.2 Matrix (delay {0,20,50}ms × loss {0,2}%), §5.3 Ergebnisse (Tabelle 5.1 + **Abb. 5.1 latency-vs-delay, Abb. 5.2 latency-vs-loss** via `\includegraphics`), Diskussion beantwortet **FF2** (Baseline 6.8ms, RT≈6.8+4.2·d) und **FF3** (2% Verlust im Mittel <50ms unmerklich, bei 50ms p95 +18% 260→307ms), §5.4 Limitierungen (Egress-only, n=5-Demo, Frisch-Dial). Sourced aus results.md. In-cycle fix: `\text{}` ohne amsmath → fatal `Undefined control sequence` → `amsmath` in Präambel. Recompiled: `BUILD_EXIT=0`, 0 undefined refs, 382KB PDF, Figuren visually confirmed. Doc-only, no Rust; 94 tests unchanged. Next: M7.6 Fazit (letztes Paket).
- **Cycle 55 — M7.4**: `chapters/implementierung.tex` (+ `listings` in preamble). §4.1 Workspace-Aufbau (5-Crate-Tabelle 4.1 mit wrappender `p{}`-Spalte; deps quinn/rustls-ring/snow/ed25519-dalek/sha2/rcgen/tokio), §4.2 ct-common Bausteine + **Listing 4.1** (asymmetrischer PoW verify/solve, wortgetreu), §4.3 Rollen-Dispatch + **Listing 4.2** (`serve_connection` `'A'`/`'C'`, gekürzt aber faithful), §4.4 Agent/Client + Bench-Modus. Sourced by reading `crates/*` (echte Signaturen/Bodies). **Metacognitive check**: die Behauptung „94 Tests" im Text empirisch gegengeprüft → `TOTAL_PASSED=94` ✓. In-cycle fix: Tabelle-Overflow → `l p{8.6cm}`. Recompiled: `BUILD_EXIT=0`, 0 undefined refs, 312KB PDF, Kapitel + Listings visually confirmed. Doc-only, no Rust; 94 tests unchanged. Next: M7.5 Evaluation.
- **Cycle 54 — M7.3**: `chapters/architektur.tex` (+ `tikz`/`positioning` in preamble). §3.1 Komponenten & Topologie (Origin/Agent/Edge/Client/Control Plane) mit **TikZ-Diagramm** (Abb. 3.1: Datenpfad Client→Edge→Agent→Origin mit QUIC/Relay/TCP-Kanten, Control Plane + gestricheltes Rendezvous, providerblinder Noise-E2E-Bogen Client↔Origin), §3.2 Schlüsselflüsse (Enrollment/Capability-Minting/Verbindungsaufbau/Revocation), §3.3 Rollen-Dispatch am Edge (`'A'` register / `'C'` PoW→route→relay — Brücke zur Implementierung), §3.4 Entwurfsentscheidungen (ADR-Verweise). From SPEC §4–5 + CONTEXT.md. Recompiled: `BUILD_EXIT=0`, 0 undefined refs, 268KB PDF (TikZ vector fig), Diagramm + Kapitel visually confirmed. Doc-only, no Rust; 94 tests unchanged. Next: M7.4 Implementierung.
- **Cycle 53 — M7.2b**: `chapters/grundlagen.tex` — §2.1 providerblinde Relays & Zero-Knowledge-Prinzip (Nutzlast blind vs. sichtbare Metadaten, opakes Routing-Token), §2.2 QUIC als Transport (`\cite{rfc9000,rfc9001}`; Multiplexing, Migration, ausgehender Aufbau, TCP-Fallback), §2.3 Noise-Protokoll (`\cite{noiseprotocol}`; `Noise_IK`, X25519-Pinning der Origin Identity, keine PKI → zensurresistent), §2.4 Proof-of-Work (`\cite{back2002hashcash}`; gestaffeltes DoS/Sybil-Schema, Grenzen offen dargelegt). Sourced from ADR-0004/0013/0018 + CONTEXT.md. In-cycle fix: two typo'd U+FFFD chars → guarded + corrected. Recompiled: `BUILD_EXIT=0`, 0 undefined refs, 208KB PDF, Grundlagen + citations [2]/[4] visually confirmed. **M7.2 complete.** Doc-only, no Rust; 94 tests unchanged. Next: M7.3 Architektur.
- **Cycle 52 — M7.2a**: decomposed M7.2 (two chapters > one pass) → M7.2a Einleitung / M7.2b Grundlagen. Wrote `chapters/einleitung.tex`: §1.1 Motivation/Problemstellung (providerblind = Betreiber aus Inhalt/Vertrauen/Datenpfad heraus; ZK auf Nutzlast begrenzt), §1.2 Zielsetzung (Rust/QUIC-Prototyp, relay path, Docker+netem-Evaluation), §1.3 Forschungsfragen (FF1 Realisierbarkeit, FF2 Latenz-Overhead, FF3 Netzverhalten), §1.4 Aufbau. Grounded in SPEC §1–3/§9 + ADR-0001/0002/0011; scientifically honest (prototype = providerblinder Relay-Pfad + PoW; Noise als Baustein in ct-common, P2P-Hole-Punching deferred). Recompiled: `BUILD_EXIT=0`, 0 undefined refs, 199KB PDF, Einleitung visually confirmed. Doc-only, no Rust; 94 tests unchanged. Next: M7.2b Grundlagen.
- **Cycle 51 — M7.1**: LaTeX thesis scaffold. `docs/thesis/thesis.tex` (`scrbook`, ngerman+english/babel, csquotes, geometry, biblatex[biber]/hyperref): HAW-Titelblatt (metadata as `\newcommand` placeholders), Eidesstattliche Erklärung, Kurzfassung (DE) + Abstract (EN), TOC, 6 chapter stubs (`chapters/*.tex`), `references.bib` (RFC 9000/9001, Noise, Hashcash). `docker/thesis.Dockerfile` (minimal TeX Live via apt: latexmk, biber, lmodern, koma/biblatex/ngerman) + `scripts/thesis-build.sh` (build image once → latexmk). **In-cycle fix**: first build failed `lmodern.sty not found` → added `lmodern` to the image. Wired the M6 `results-table.tex` into the Evaluation chapter so `\ref{tab:latency}`→`Tabelle 5.1` resolves. Verified: `BUILD_EXIT=0`, 189KB `thesis.pdf`, **0 undefined refs**, title page + citations [1]–[4] + table visually confirmed. Doc/data-only, no Rust; 94 tests unchanged. Next: M7.2 Einleitung+Grundlagen.
- **Cycle 50 — M6.4**: `scripts/tabulate.py` (stdlib, runs in `python:3-slim`, no deps) — CSV → `results-table.md` (GH-Markdown) + `results-table.tex` (LaTeX booktabs, German headers, `\label{tab:latency}`), sorted by (delay,loss), auto rate-column. Ran a real **3×2 matrix** sweep (delay {0,20,50}ms × loss {0,2}% × n=5), regenerated both figures (loss plot now populated), and wrote `results.md` (German analysis): baseline 6.8ms; **RT ≈ 6.8 + 4.2·delay** (multi-hop relay + fresh-dial handshake cross the delayed edge egress ~4× per round-trip); 2% loss negligible at low RTT but grows the p95 tail at 50ms (260→307ms). Tables + tex + figures visually verified. Script/data-only, no Rust; 94 tests unchanged. **M6 complete.** Next: M7.1 LaTeX thesis scaffold.
- **Cycle 49 — M6.3**: `scripts/plot.py` (headless matplotlib) + `scripts/plot.sh` (runs it in a `python:3-slim` container, `pip install --user matplotlib`, uid-mapped so PNGs are host-owned — docker-only, no host install). Reads `latency.csv` → `latency-vs-delay.png` (mean vs edge delay, one series per loss, p95 upper error bar, German labels) and conditionally `latency-vs-loss.png` (skipped with <2 distinct loss values). Verified: rendered a real 37KB PNG (visually confirmed — 0ms→7ms, 20ms→96ms with error bar). Script-only, no Rust; 94 tests unchanged. Next: M6.4 results tables.
- **Cycle 48 — M6.2**: `scripts/sweep.sh` — builds the testbed image once, then runs the compose testbed across a `SWEEP_DELAYS × SWEEP_LOSSES × SWEEP_RATES` matrix (env-configurable; `CLIENT_ITERATIONS`), greps each run's `RESULT` line and appends it to `SWEEP_OUT` (default `docs/thesis/data/latency.csv`), `compose down -v` between conditions. Script-only (no Rust) → acceptance is a produced CSV (per the M5.4/M5.5 docker-packet convention). **Live 2-condition run**: `0ms,0%,,3,7.131,…` and `20ms,0%,,3,95.638,…` — committed as the first real dataset. Unit tests unchanged (94 green). Next: M6.3 plots.
- **Cycle 47 — M6.1c**: `ct-client` `main` bench mode — `CT_CLIENT_ITERATIONS>1` → `run_bench` → print `RESULT <csv_row>` labeled with `CT_BENCH_{DELAY,LOSS,RATE}`; compose exposes `CLIENT_ITERATIONS` + bench labels. **Live verification** (`CLIENT_ITERATIONS=5 EDGE_DELAY=20ms`): `netem: applying delay 20ms`, `RESULT 20ms,,,5,92.917,88.621,108.943,89.021,108.943`, `COMPOSE_EXIT=0` — **first real perf data from the testbed** (mean 92.9ms round-trip). Unit tests 94 green. Committed. Next: M6.2 sweep script.

## Verification method

Local green bar per cycle = `cargo build --workspace` + `cargo test --workspace` inside `rust:1-slim` (host has no cargo; docker is the hermetic runner, per D3). `cargo fmt`/`clippy`/`audit` run in CI (`ci.yml`); added to the local gate once components are baked into `Dockerfile.dev` (a later packet).
